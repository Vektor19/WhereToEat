# developer notes — frontend-app

## Step 11 (query-controls)
- New feature dir: `web/projects/app/src/app/consumer/query-controls/`
  - `match-control.component.ts` (dumb): Or/And button-toggle; And labelled "Комбо (І)"; `modeChange` output (NOT `change` — collides with native DOM event per `@angular-eslint/no-output-native`).
  - `sort-control.component.ts` (dumb): five contract sort modes; data-driven `options` with `explicit` flag; helper `hint` carries explicit-sort-dominates copy (coverage = tie-breaker) vs composite-balance copy. `data-explicit` attr on hint for tests. `modeChange` output.
  - `filters-control.component.ts` (dumb): data-driven via `FilterDescriptor[]` (`DEFAULT_FILTER_DESCRIPTORS` = price/rating). New filter key = append descriptor, no code change (invariant #4). Empty input emits `clear`, non-empty emits `apply`.
  - `query-controls.component.ts` (smart): injects SelectionStore + ResultsStore; submit = `selection.buildRequest()` (Step 8 tryBuild) → on invalid shows typed ValidationErrors + blocks; on valid `results.submit()`. submitDisabled = !hasSelection || loading. Emits `filter` analytics via INGEST_ANALYTICS (best-effort, replaced by Step 16 emitter).
- Wired `<app-query-controls />` into `selection-builder.component.ts` (the consumer route entry).
- IMPORTANT side effect: embedding query-controls makes SelectionBuilderComponent transitively need RECOMMEND/ResultsStore. Had to add `ResultsStore` + `{provide: RECOMMEND, useValue: INERT_RECOMMEND}` to BOTH TestBed configs in `selection-builder.component.spec.ts`.
- Outcomes: app 65/65, core 273/273, lint clean, prettier clean, `ng build app` ok (pre-existing shell budget warning unrelated).

## Step 13 (inline View-on-map, swappable map provider)
- New `core/src/map/`: `map-provider.ts` (`MapProvider` interface, `MapView` discriminated union `embed|deep-link|none`, `MAP_PROVIDER` token); `deep-link.provider.ts` + `google-embed.provider.ts` (both pure — shape the live payload only, no I/O/caching); `map-provider.selection.ts` (`selectMapProvider` pure factory + `provideMapProvider()` EnvironmentProviders). Embed provider builds `/embed/v1/place` (Place ID) or `/embed/v1/view` (coords) with config key; degrades to DeepLink provider when no key or no embeddable target. Barrel `map/index.ts` + added to `public-api.ts`.
- NAME CLASH: config token's `MapProvider` string-union renamed → `MapProviderKind` (was only used inside app-config.token.ts). The `MapProvider` interface now owns that name.
- `provideMapProvider()` added to `app.config.ts` root providers (binds MAP_PROVIDER per config).
- `MapModalComponent` (smart) `consumer/map/map-modal.component.ts`: fetches via `GET_MAP` + shared `LoadingService`/`RequestState`; resolves through `MAP_PROVIDER`; renders sandboxed iframe (`sandbox="allow-scripts allow-same-origin"`, `referrerpolicy="no-referrer"`, src via `DomSanitizer.bypassSecurityTrustResourceUrl`), deep-link affordance, or no-map empty state (shared error-state). ARIA `role=dialog`/`aria-modal`, CDK `cdkTrapFocus cdkTrapFocusAutoCapture`, ESC via component `host` listener (NOT on the non-focusable backdrop — interactive-supports-focus rule), backdrop click closes only on `target===currentTarget`. Output is `closed` (NOT `close` — no-output-native). Emits one `action` event on open (best-effort, Step 16 emitter supersedes).
- Result card: added `viewMap` output + "Глянути на карті" button (separate from `open`/details). Ad-slot re-emits `viewMap`. Results-list hosts `<app-map-modal>` via `mapTarget` signal; `onViewMap` records `document.activeElement` as invoker, `onMapClose` returns focus.
- Added `--dp-color-scrim` token to `_tokens.scss` (M3 scrim role) — modal backdrop must not use a raw rgba.
- Results-list spec + result-card spec needed `GET_MAP`/`MAP_PROVIDER` providers now that the modal is a child.
- Outcomes: core 288/288, app 102/102, lint clean, prettier clean, build green. Note: result-card component-style budget now warns (4.55kB vs 4kB soft warn) like the pre-existing app-shell warning — soft warning only, no error.

## Step 14 (restaurant details — always-free contact links, menu, generic photos)
- New `consumer/details/`: `details.view-model.ts` (RestaurantDetailsViewModel / ContactLinkViewModel / MenuItemViewModel); `contact-links.component.ts` (dumb, `nav` of safe external links — web links `target=_blank rel="noopener noreferrer"`, phone links `tel:` in place; ALWAYS rendered even for non-Verified, empty→discreet note never paywall; `linkClick` output for the action event); `menu-list.component.ts` (dumb, reuses `../results/generic-photo.component`, price pre-formatted by container, weight only when present, generic photos default); `restaurant-details.component.ts` (smart, routed leaf).
- Container fetches `GET_RESTAURANT_DETAILS` via shared `LoadingService`/`RequestState` (loading/error/404-not-found reuse shared primitives). Currency via `CurrencyFormatter.format(amount, priceCurrency)` (never hardcoded). Emits `view`+`card_open` once per id load + `action` on contact click (best-effort INGEST_ANALYTICS, Step 16 emitter supersedes). Back nav via injected `Location.back()` (no output — it's the routed leaf).
- ROUTING: route `restaurant/:id` added to `consumer.routes.ts`. Enabled `withComponentInputBinding()` in `app.config.ts` so `:id` binds to the container's `id` input (global router config change, low-risk; required for routed `input()`). Results-list `onOpen` now `router.navigate(['restaurant', id])` after the card_open event — added `Router` + `provideRouter([])` to results-list spec, asserted navigate call.
- CATALOG: added read-only fetch-free `dishNameById(dishId): Signal<string|undefined>` to `catalog.store.ts` (Step 9 file — carry-over explicitly authorized; scans cached per-category dishes, no load). Menu falls back to neutral `'Страва'` label when taxonomy not loaded (avoids forcing a full taxonomy fetch on details).
- Details spec stubs `CatalogStore` (StubCatalogStore) to avoid pulling APP_CONFIG/HTTP graph; resolves only `d-1`→Борщ to exercise resolve + fallback paths.
- Outcomes: core 291/291 (29 files), app 124/124 (19 files), lint clean, prettier clean, build green (restaurant-details lazy chunk 11.39kB).

## Step 15 (opt-in geolocation + distance ranking toggle)
- New `core/src/geo/geolocation.service.ts` (`GeolocationService`): the ONLY place `navigator.geolocation` is touched (RN-portable seam). `acquire(): Promise<GeolocationResult>` — never rejects; success carries `GeoCoordinate`, failures are typed (`permission-denied|position-unavailable|timeout|unsupported`) with a stable i18n `messageKey` (`GEO_MESSAGE_KEYS`, keys land in Step 17). Requests `enableHighAccuracy:false` (approximate only — invariant #11), 10s timeout, 5min maxAge.
- New `core/src/geo/geo-optin.store.ts` (`GeoOptInStore`): persists ONLY the boolean opt-in choice in `localStorage` (`dp.geo.optIn`), default OFF. Coordinates are NOT persisted (re-acquired on opt-in to avoid stale position). Signal `optedIn` + `optIn()`/`optOut()`. Both exported via existing `geo/index.ts` barrel → public-api.
- New `app/src/app/consumer/geo/geo-optin.component.ts` (`GeoOptInComponent`, smart): Material slide-toggle + discreet consent copy + error line. On toggle-on → `acquire()` → success: `SelectionStore.setGeo` + `optInStore.optIn()`; failure: `clearGeo`+`optOut` + show `messageKey`. On toggle-off → `clearGeo`+`optOut`. On init, if persisted opt-in is on and no geo yet, SILENTLY re-acquires (no re-prompt while permission stands; reverts to off + NO error toast if revoked). Toggle disabled while `acquiring`.
- WIRING: `<app-geo-optin />` added to `query-controls.component.ts` (after sort control). `selection-builder.component.ts` `emitSearch()` now attaches `userGeo` (when present) as `geo` on the `search` event builder → rides as lat/lng FIELDS (no `geo` kind — invariant #11). Added `localStorage.clear()` to query-controls + selection-builder specs (geo store reads it on init).
- TESTS: `geolocation.service.spec.ts` (stubs `navigator.geolocation`, all 4 failure mappings + unsupported + low-accuracy assertion); `geo-optin.store.spec.ts` (default off, persist across instances); `geo-optin.component.spec.ts` (default off → no acquire; opt-in → setGeo + valid userGeo in built request; off → clearGeo + request omits userGeo; denied → message key + no opt-in; a11y toggle+consent; persisted re-acquire; revoked-reverts-silently). selection-builder spec gained geo-on-search-event capture (lat/lng present when opted-in, absent otherwise, never a `geo` kind).
- Outcomes: core 299/299 (31 files), app 134/134 (20 files), lint clean, prettier clean, build green. Pre-existing soft style-budget warnings (result-card 4.55kB, app-shell 4.36kB) unchanged/unrelated.

## Step 16 (consumer analytics emission wiring — single batching emitter)
- New `core/src/analytics/analytics-emitter.service.ts` (`AnalyticsEmitterService`, providedIn root): the ONE emission path. `emit(...events)` buffers; flushes on size (`FLUSH_BATCH_SIZE=20`), debounced time (`FLUSH_DEBOUNCE_MS=5000`, reset per emit), or page hide (`visibilitychange`→hidden + `pagehide`), or injector destroy. `flush()` clears buffer+timer up-front then fire-and-forgets the batch via `INGEST_ANALYTICS` with `catchError(()=>EMPTY)` — failure is swallowed AND the batch is dropped (no unbounded retry queue). Defensive `KNOWN_KINDS` guard drops any non-eight-kind event so the ingest batch can never 400 on `geo`/unknown. `emitImpressionsOnce(token, impressions)` dedupes one impression per restaurant per result-set token (new token resets scope). Imports the `INGEST_ANALYTICS` token directly from `../data-access/tokens` (one-way analytics→data-access edge, no cycle). Exported via `analytics/index.ts` barrel → public-api.
- REWIRED off direct `INGEST_ANALYTICS` onto the emitter (removed each component's private `emit()` helper + the `ingest`/`destroyRef`/rxjs `EMPTY`/`catchError`/`takeUntilDestroyed` imports they only used for it):
  - `results-list.component.ts`: impressions via `emitter.emitImpressionsOnce(resultSetToken())` (token = `JSON.stringify(results.request())`); `card_open` via `emitter.emit`.
  - `map-modal.component.ts`: `action` via `emitter.emit` (actionEmitted guard unchanged).
  - `restaurant-details.component.ts`: `view`+`card_open` and contact `action` via `emitter.emit`.
  - `query-controls.component.ts`: `filter` via `emitter.emit`.
  - `selection-builder.component.ts`: `search` via `emitter.emit` (geo lat/lng still ride as FIELDS, no `geo` kind). NOTE: it still uses rxjs `EMPTY`/`catchError`/`takeUntilDestroyed`+`DestroyRef` for the typeahead — only the rxjs `EMPTY` import was dropped (was used solely by the removed emit helper); kept the rest.
- `rating_given` slot: the emitter accepts the full 8-kind union; Step 22 calls `emitter.emit(events.ratingGiven(...))` on submit — no change needed here.
- TESTS: new `analytics-emitter.service.spec.ts` (batches→one flush; size-threshold immediate flush; debounce-timer flush via `vi.useFakeTimers`; pagehide/visibilitychange flush; failure swallowed + dropped-not-retried; unknown-kind dropped; only-eight-kinds-never-geo; impression dedupe per result set). The 5 rewired component specs now `TestBed.inject(AnalyticsEmitterService).flush()` before reading the `INGEST_ANALYTICS` stub captures (buffer no longer flushes synchronously). selection-builder spec gained a `flushAnalytics()` helper.
- Outcomes: core 307/307 (32 files), app 134/134 (20 files), lint clean, prettier clean, build green. Pre-existing soft style-budget warnings (result-card 4.55kB, app-shell 4.36kB) unchanged/unrelated.

## Step 17 (i18n plumbing — reconciled a partial interrupted attempt + completed)
- Approach: `@ngx-translate/core` v18 RUNTIME catalogs (not compile-time `$localize`) so the wired switch swaps locale with no rebuild. Catalogs live in `core/src/i18n` (view-independent, RN-portable).
- Pre-existing (from interrupted attempt, kept): `core/src/i18n/{locale.config,translation-catalog,in-memory-translate.loader,locale.service,index}.ts`; `catalogs/{uk,en}.catalog.ts` (uk fully populated, en `{}` placeholder proving additive locales = config-only); `LocaleService.use()` drives `translate.use()` + `document.documentElement.lang` + localStorage persist (`dp.locale`) + `activeLocale` signal; `app.config.ts` wires `provideTranslateService` (in-memory loader, uk lang+fallback) + `LOCALE_ID` + `registerLocaleData(uk)` + `LocaleService.init()` initializer; real `LanguageSwitchComponent` replaced the shell placeholder; error-state/loading/match-control/sort-control/result-card/results-list/menu-list/contact-links/ad-slot already catalog-keyed.
- BROKEN half-done work I FIXED: `filters-control.component.ts` template referenced `descriptor.label`/`.hint` but the type had been renamed to `labelKey`/`hintKey` (compile error blocked the whole app build) — rewired template to `| translate` + `clearAria` via `TranslateService.instant`. Spec line used `label:` → `labelKey:`.
- LEFTOVER raw `uk` literals I folded into the catalog + rewired to `| translate`/`instant`: `restaurant-details` (loadingLabel/emptyLabel/backLabel + DISH_NAME_FALLBACK 'Страва' → `consumer.details.*`/`common.back`; ALSO now passes active locale to currency formatter so menu prices follow the switch — was hardcoded uk), `selection-builder` (tab labels), `selection-summary` (title/region/clear/empty/removeAria), `category-browser` (all browse labels + 3 aria methods), `prefix-search` (label/placeholder/clear/results/loading/empty), `query-controls` (submitLabel), `map-modal` (loading/close/noData/deepLink*/titlePrefix), `geo-optin` (toggle/acquiring/consent + 4 `consumer.geo.error.*`). Added `common.empty:''` for the geo no-error reactive-translate placeholder.
- TEST HARNESS (the load-bearing infra fix): app component specs assert real translated text but had NO TranslateService → 105 failures. Fixed with `projects/app/src/test-providers.ts` (default-export provider array wiring `provideTranslateService` + in-memory loader, referenced via the app test target's `providersFile`). ngx-translate is partially-AOT-compiled and its module-init needs the JIT compiler, which the unit-test env lacks → added an `app:build:test` build configuration with `"polyfills":["@angular/compiler"]` and pointed the test target's `buildTarget` at it. (Tried setupFiles/runnerConfig optimizeDeps first — only the compiler polyfill worked, because `init-testbed.js` imports the providersFile before any setup file runs.) Added `test-providers.ts` to `tsconfig.spec.json` include.
- TESTS added: `i18n/locale.config.spec.ts`, `i18n/in-memory-translate.loader.spec.ts` (known uk keys resolve incl. all deferred errors/rating/geo keys; unknown locale → `{}` fallback), `i18n/locale.service.spec.ts` (init fallback+persisted, use() swaps catalog+lang+signal+persist, unsupported ignored — via TranslateService/DOCUMENT stubs, no ngx-translate runtime), `shell/language-switch.component.spec.ts`.
- Outcomes: core 324/324 (35 files), app 143/143 (22 files), lint clean, prettier clean, `npm run build` green. Pre-existing soft budget warnings (initial bundle, result-card, app-shell styles) unchanged/unrelated. Step 8 formatter specs already cover locale-aware km/currency/number — left as-is.

## Step 17 revision (3 developer-reviewer findings)
- MEDIUM (LOCALE_ID comment): `app.config.ts` comment now states `LOCALE_ID` is PINNED to `uk` and does NOT follow the runtime language switch (static provider → Angular built-in DatePipe/DecimalPipe/CurrencyPipe would always format uk). Safe today because the app uses NONE of those locale-sensitive pipes — all locale-aware output goes through the Intl-based `core` formatters which follow `LocaleService.activeLocale()`. Comment warns: adding an Angular locale-aware pipe later requires making this provider follow the active locale (factory reading LocaleService) or output silently mismatches.
- LOW #1 (Signal<string> cast invariant): centralized into ONE shared helper `core/src/i18n/translate-text.ts` → `translateText(translate, key, params?)`: wraps `TranslateService.translate(...)` (mirrors its static/signal/arrow key+params signature) and narrows the return to `Signal<string>` in a single documented place. Doc comment states the invariant: every catalog key in this app is a leaf string (flat dotted keys, leaves are strings — see TranslationCatalog), so the resolved value is always a string and the cast is sound; a future sub-tree key has one obvious place to revisit. Exported via `i18n/index.ts` → public-api. Migrated ALL 14 inline `as Signal<string>` casts to it: result-card (5), error-state (2), match-control (1), sort-control (1), restaurant-details (3), geo-optin (1), map-modal (1). Removed now-unused `type Signal` imports from those components (map-modal also dropped its separate `import type { Signal }` line).
- LOW #2 (implicit global test-providers dependency): added a comment in `ad-slot.component.spec.ts`, `sort-control.component.spec.ts`, `match-control.component.spec.ts` noting the TranslateService is supplied GLOBALLY via `projects/app/src/test-providers.ts` (providersFile in angular.json), so the uk-text assertions render translated copy rather than raw keys and the specs deliberately register no local translation providers.
- Outcomes: `npm run build` green; `npm run lint` green (app+core); `npm run format` (prettier --check) green; `npm test` 143 passed / 22 files. Pre-existing soft budget warnings unchanged. Step left [~] in-progress for qa.

## Step 18 (operator portal shell + role-gated lazy boundary)
- New `app/src/app/portal/`: `portal-shell.component.ts` (smart chrome — top bar with brand + signed-in identity (`portal-subject`/`portal-role`) + login/logout wired to `core` `AuthService`, Material side-nav, content `<router-outlet>`; renders the `portal-denied` `role=alert` state for an authenticated non-admin instead of the nav; skip link to `#dp-portal-content`); `portal-home.component.ts` (the guarded landing/dashboard placeholder Steps 19/20/24 replace/extend); `portal-nav.ts` (`PORTAL_NAV` data-driven nav list + `PortalNavItem` — features add an entry here, not edit the shell; `testId` decoupled from copy/path for stable `data-testid="portal-nav-<testId>"`).
- ROUTING: `portal.routes.ts` now a single PARENT route `canActivate:[adminGuard]` + `canActivateChild:[adminGuard]`, `loadComponent` → PortalShellComponent, with a child `''` `loadComponent` → PortalHomeComponent. Guard on the parent gates the WHOLE subtree once; future feature routes are siblings of the home child and inherit the gate. `adminGuard`/`AuthService` come from the `core` public-api barrel (NOT a deep path — boundary rule). Root `app.routes.ts` already lazy-imports `portal.routes` (unchanged).
- i18n: added a `portal` section to `uk.catalog.ts` (brand/skipToContent/navLabel/signIn/signOut + `portal.nav.*` 8 keys + `portal.home.*` + `portal.denied.*`). `en.catalog.ts` stays the empty additive-proof placeholder.
- TOKEN: added `--dp-layout-portal-nav-width: 16rem` to `_tokens.scss` layout section (side-nav width; no raw px in the component).
- BOUNDARY (verified, not just configured): a temp `consumer/__boundary_probe.ts` importing `../portal/portal-shell.component` made `ng lint app` fail with `consumer is not allowed to import portal  boundaries/element-types` — then deleted. Build output confirms `portal-shell-component`/`portal-home-component`/`portal-routes` are SEPARATE lazy chunks from `consumer-routes`/`selection-builder-component`.
- TESTS: `portal-shell.component.spec.ts` (AuthServiceStub with writable signals — admin nav renders all 8 entries; subject/role shown; logout→`AuthService.logout`; anonymous→login affordance wired to `AuthService.login`; non-admin→`portal-denied` `role=alert` + no nav but identity still shown; skip link; no "updated" copy). `portal.routes.spec.ts` (parent uses `loadComponent` not eager `component`; child lazy; `canActivate` contains the exact `core` `adminGuard`; router nav: admin admitted, non-admin blocked w/o login, anonymous blocked + login initiated).
- Outcomes: `npm run build` green; `npm run lint` green; `npm run format` green; `npm test` → core 324/324 (35 files), app 155/155 (24 files). Portal-shell component-style budget warns 5.39kB>4kB (soft warning, consistent with pre-existing app-shell/result-card warnings — non-blocking). Step left [~] in-progress for qa.

### Step 18 review fixes (developer-reviewer, 2 MEDIUM, tests only)
- Issue 1 (guard identity): `portal.routes.spec.ts` "guards the whole subtree" test now asserts `expect(route.canActivateChild).toContain(adminGuard)` in addition to the prior `length === 1` — a wrong/permissive guard of the right arity would now fail. `canActivate` already had the `toContain(adminGuard)` assertion.
- Issue 2 (canActivateChild exercised via CHILD path): added a nested `describe('child deep-link inherits the gate via canActivateChild')`. A `routesWithTestChild()` helper spreads the REAL portal parent route (preserving its actual `canActivate`/`canActivateChild`/lazy `loadComponent`) and appends a throwaway eager child `{ path: 'menu', component: TestChildComponent }` (empty template — guard fires before any child component loads). The nested `beforeEach` calls `TestBed.resetTestingModule()` BEFORE re-configuring, because the outer `beforeEach` already instantiated TestBed (via `TestBed.inject(Router)`) and Angular forbids reconfiguring an instantiated module. Three nav tests to `/menu`: admin → allowed (`url==='/menu'`); authed non-admin → blocked (`false`, url not `/menu`, no login); anonymous → blocked + `login` called once. This proves the inheritance Steps 19/20/24 children rely on.
- `TestChildComponent` selector is `app-test-child` (not `pos-*`) to satisfy the `@angular-eslint/component-selector` `app` prefix rule.
- Outcomes: `ng test app` 158/158 (24 files; portal spec now 8 tests, up from 5); `npm run build` green; `ng lint app`+core green; `npm run format` green. Step left [~] in-progress for qa.

## Step 19 (portal management features — menu / protection / address / photos)
- SHARED portal-internal primitives in `app/src/app/portal/shared/` (portal-only — never crosses into consumer; boundary keeps them apart):
  - `save-feedback.component.ts` (dumb): the ONE write-result strip (pending spinner / `save-success` role=status / `save-error` role=alert from typed `ApiError.messageKey` via `translateText`). Distinct from consumer `ErrorStateComponent` (that's a READ full-panel w/ retry; this is a WRITE inline strip incl. saved-OK). Error preferred over saved.
  - `restaurant-picker.component.ts` (dumb): "enter restaurant id → Load" form (no admin read endpoint exists, so operator addresses by id; host fetches `GET /restaurants/{id}`). Output is `loadId` (NOT `load` — collides with native DOM `load` event per `@angular-eslint/no-output-native`). Emits trimmed non-blank id; load button disabled while host loading or blank.
- MENU (`portal/menu/`): `menu-edit-form.component.ts` (dumb, ReactiveForms) — Category→Dish taxonomy picker (invariant #2): category `<mat-select>` narrows dish list; emits exact `EditMenuItemBody` (`dishId`/`priceAmount`/`priceCurrency` upper-cased/`weight` null-when-blank). `categoryChange` output asks container to load that category's dishes. `menu-management.component.ts` (smart) — fetches `GET_RESTAURANT_DETAILS` via shared `RequestState`; lists items; selecting one seeds the form (category resolved from cached taxonomy via `CatalogStore.dishes(cat)()` scan); `EDIT_MENU_ITEM.execute(itemId, body)` 204→`save-success`, throw→typed `save-error`. Uses `CatalogStore` (loadCategories cached; loadDishes per category).
- PROTECTION (`portal/protection/`): `protection-flags.component.ts` (smart) — restaurant-level do-not-update (`SET_RESTAURANT_DO_NOT_UPDATE`) + per-item do-not-parse (`SET_MENU_ITEM_DO_NOT_PARSE`), both send `{value}`. NO admin read of current flag values (no endpoint), so each toggle is an explicit SET: optimistic flip → on error REVERTS to prior value + shows per-row typed error (keyed `FlagState` map for items; single signal for restaurant). 204→cleared error.
- ADDRESS (`portal/address/`): `address-management.component.ts` (smart, ReactiveForms inline) — pre-fills line+city from `GET /restaurants/{id}`; `EDIT_ADDRESS.execute(id, {addressLine, city|null})` 204→saved; city null-when-blank. Shows discreet re-geocode note (invariant #7 — server re-derives OSM coords; client never sends coords).
- PHOTOS (`portal/photos/`): `photo-permission.component.ts` (smart, ReactiveForms) — photo id field + permission slide-toggle (default OFF = generic stays default, invariant #8); `SET_PHOTO_PERMISSION.execute(photoId, value)` `{value}` 204→saved. No read endpoint → operator enters photo id directly.
- ROUTING: each feature is its OWN `*.routes.ts` (`menuRoutes`/`protectionRoutes`/`addressRoutes`/`photosRoutes`); `portal.routes.ts` registers them as `loadChildren` siblings of the home child (each its own lazy chunk, inherits the parent's `adminGuard`). `PORTAL_NAV` already linked these paths (Step 18) — NO shell/nav edit needed.
- i18n: added `portal.save.*`, `portal.picker.*`, `portal.menu.*`, `portal.protection.*`, `portal.address.*`, `portal.photos.*` to `uk.catalog.ts`. Copy makes admin>parser (protection) + generic-default (photos) + re-geocode (address) explicit.
- TESTS: 6 new specs (4 feature containers + 2 shared dumb). Mock the data-access ports (`{execute: ...}` useValue) + stub `CatalogStore` (menu). Each: correct port called with exact body/id, 204→success strip, throw(ApiError)→typed error strip; menu re-categorise sends new dishId; protection sends `{value}` + reverts on fail; address null-city; photos default-off. Plus `portal.routes.spec.ts` extended: asserts menu/protection/address/photos children registered with `loadChildren`. Note `typeof of<void>` is invalid — used `Observable<void>` for the result-fn types.
- Outcomes: `npm run build` green (4 separate lazy chunks: menu-management 46.5kB raw / protection-flags 7.0kB / address-management 5.4kB / photo-permission 3.6kB); `npm run lint` app+core clean; `npm run format` clean; `npm test` → core 324/324 (35 files), app 188/188 (30 files; +30 over Step 18's 158). Pre-existing soft style-budget warnings (portal-shell 5.39kB, result-card 4.58kB, app-shell 4.39kB, initial bundle) unchanged/unrelated. Step left [~] in-progress for qa.

## Step 20 (portal: Verified subscription + labeled ad placements)
- New dirs: `web/projects/app/src/app/portal/verified/` (`verified.component.ts`, `verified.routes.ts`, spec) and `portal/ads/` (`ad-placement.component.ts`, `ads.routes.ts`, spec).
- Registered both as lazy `loadChildren` siblings under the existing admin-gated portal parent in `portal.routes.ts`. Nav entries (`verified`/`ads`) already existed in `portal-nav.ts` from Step 18 — no nav change needed.
- VerifiedComponent (smart): injects GRANT_VERIFIED + REVOKE_VERIFIED. Venue-id text field + mat-select tier (Basic/Pro). Grant button gated on `form.invalid`; revoke button gated on `venueIdBlank()` (revoke needs only the id). The UI passes the readable `VerifiedTier` ('Basic'/'Pro') to the port; the Step-5 data-access port does the integer-ordinal mapping (Basic=1/Pro=2) — component does NOT map ordinals. Single shared `saveState` (LoadingService) drives SaveFeedbackComponent for whichever action ran. Partner-status compliance copy via `portal.verified.partnerNote`.
- AdPlacementComponent (smart): injects CREATE_AD_PLACEMENT. Group-level `dateRangeValidator` enforces `startsAt < endsAt` (returns `{dateRange:true}`); `datetime-local` inputs converted to ISO via `new Date(v).toISOString()` for the wire body. 201 `{id}` lands in `saveState.data` → `created()` confirmation strip (`ad-created` / `ad-created-id`). SaveFeedback `[saved]="false"` here because the dedicated created-strip is the success surface. Labeled-paid-slot compliance copy via `portal.ads.labeledNote`.
- i18n: added `portal.verified.*` and `portal.ads.*` keys to `web/projects/core/src/i18n/catalogs/uk.catalog.ts` (after the `photos` block). Spec copy assertions match Ukrainian substrings ('партнерського', 'не «знак якості від нас»', 'платний рекламний слот', '«Реклама»', 'Органічний рейтинг не продається').
- GOTCHA: `ApiError.messageKey` is the closed `ApiErrorMessageKey` union (errors.validation/unauthorized/forbidden/notFound/server/network/unknown) — no `errors.badRequest`. Used `errors.validation` for the 400 fixture.
- Extended `portal.routes.spec.ts` lazy-children assertion to include `verified`/`ads`.
- Outcomes: build OK (both chunks emitted); app 202/202, core 324/324; lint clean; prettier clean.

## Step 21 (backend: Ratings write port + Dapper adapter + Ratings.Application submit use-case)
- NEW write port `IRatingRepository` in `src/Modules/Ratings/WhereToEat.Ratings.Domain/Abstractions/IRatingRepository.cs`: `GetByRestaurantAndUserAsync` + `AddAsync` + `UpdateAsync` (revise-vs-create). Sits beside `IRatingAggregateRepository`, does not replace it.
- Dapper adapter `DapperRatingRepository` (public) in `Ratings.Infrastructure/Persistence/`; new SQL constants in `RatingsSql.cs` (SelectRatingByRestaurantAndUser / InsertRating / UpdateRating). New internal `RatingRow`. Rehydrates via `Rating.Create(RatingId,...)` overload. Schema UNCHANGED (ratings.Rating already has UQ_Rating_Restaurant_User + 1..5 check).
- NEW project `WhereToEat.Ratings.Application` (Domain+SharedKernel+Contracts only, MassTransit-free): `Submit/SubmitRatingCommand.cs` (RestaurantId, UserId, Score — refs already resolved by Step 22 endpoint), `Submit/SubmitRatingCommandHandler.cs` (load→Revise or Create→persist→publish RatingGiven; uses `System.TimeProvider` for GivenAt). Publish seam port `Abstractions/IRatingGivenPublisher.cs`.
- MassTransit adapter `MassTransitRatingGivenPublisher` (uses `IPublishEndpoint`) lives in `Ratings.Infrastructure/Messaging/`. Infrastructure now references MassTransit pkg + the Application project.
- KEY DIVERGENCE FROM PLAN (flagged in implementation.md Step 21): the `AddRatingsApplication(connectionString)` DI extension is in **Ratings.Infrastructure** (`DependencyInjection/RatingsApplicationServiceCollectionExtensions.cs`), NOT in Ratings.Application — because it registers the Infrastructure Dapper+MassTransit adapters, which would break the `Application_DependsOnNoInfrastructureAndNoHost` arch fitness test if placed in Application. Mirrors existing `AddAdminModule` (lives in Admin.Infrastructure). `AddRatingsPersistence` (Worker path) LEFT UNCHANGED.
- AddRatingsApplication registers: `TryAddSingleton<ISqlConnectionFactory>`, `TryAddSingleton(TimeProvider.System)`, scoped IRatingRepository→DapperRatingRepository, scoped IRatingGivenPublisher→MassTransitRatingGivenPublisher, scoped SubmitRatingCommandHandler. Requires host to have wired MassTransit (AddMessaging) — PublicApi does.
- STEP 22 wiring: call `builder.Services.AddRatingsApplication(connectionString)` in `WhereToEat.PublicApi/Program.cs` (NOT AddRatingsPersistence). Endpoint maps IIdentityContext.SubjectId → SubmitRatingCommand.UserId, path restaurantId → RestaurantId, body {score} → Score. Handler returns Result (Validation failure "Rating.ScoreOutOfRange" → 400 {error,message}).
- TESTS: `tests/Unit/WhereToEat.Ratings.Application.UnitTests` (6 tests, NSubstitute mocks + private FixedTimeProvider): create, revise-in-place, out-of-range create (theory 0/6/-1) no persist/publish, revise-to-out-of-range no publish. New write-side integration tests in existing `tests/Integration/WhereToEat.Ratings.Integration/RatingPersistenceTests.cs` (insert+get roundtrip, revise updates same row not duplicate). Both new test projects/files added to src/WhereToEat.sln.
- OUTCOMES: `dotnet build src/WhereToEat.sln` 0 warn / 0 err. Application unit 6/6 pass; Ratings.Domain 29/29; Architecture 34/34. Integration tests COMPILE but require Docker (unavailable in this env — same as the pre-existing aggregate integration suite); they could not be executed here.

## Step 22 (backend authenticated POST rating endpoint + frontend rating UI wiring)
- BACKEND endpoint `src/Hosts/WhereToEat.PublicApi/Endpoints/RatingEndpoints.cs`: `POST /restaurants/{restaurantId:guid}/ratings` body `{ score }`, `.RequireAuthorization(AuthServiceCollectionExtensions.UserPolicy)` (401 anonymous). Maps `IIdentityContext.SubjectId` (string `sub`) → opaque user Guid via internal `OpaqueUserId.FromSubject` (SHA-256 first 16 bytes — deterministic + non-reversible, raw sub never stored, invariant #11). Drives the Step 21 `SubmitRatingCommandHandler`; success → **204 NoContent**, Result.Validation failure (`Rating.ScoreOutOfRange`) → **400 `{error,message}`**. Defensive blank-sub → 401.
- DI: added `AddRatingsApplication(connectionString)` (NOT AddRatingsPersistence) to `WhereToEat.PublicApi/Program.cs` AFTER `AddMessaging` (the MassTransit RatingGiven publisher needs IPublishEndpoint). `app.MapRatingEndpoints()` after the auth-probe map. Added Ratings.Application + Ratings.Infrastructure ProjectReferences to the PublicApi csproj.
- BACKEND TESTS: `tests/Integration/WhereToEat.PublicApi.Integration/RatingEndpointTests.cs` (mirrors AuthTests/AnalyticsEndpointTests, same collection/fixture, TestJwt.Valid): 401 anonymous, 204 + persisted raw `ratings.Rating` fact (use-case ran, opaque UserId not the raw sub), submit-twice revises same row (count==1, score overwritten), 400 + `Rating.ScoreOutOfRange` envelope on score 6. COMPILES + wired but requires Docker (unavailable here, same as all PublicApi integration tests).
- FRONTEND data-access `web/projects/core/src/data-access/public/rating.operation.ts` (`SUBMIT_RATING` token + `HttpSubmitRatingOperation`): `POST publicUrl('/restaurants/{id}/ratings')` body `{score}` → 204. Barrel + token registered. Bearer attached by interceptor.
- BEARER ALLOWLIST FIX (load-bearing): the endpoint path is `/restaurants/{id}/ratings`, NOT top-level `/ratings`, so the existing `startsWith('/ratings')` prefix rule did NOT match it (and would have wrongly tokenized `/restaurants/{id}` details GET if loosened). Reworked `AUTHENTICATED_PUBLIC_PATHS` → `AUTHENTICATED_PUBLIC_RULES` with `match: 'prefix'|'suffix'`; `/me` stays prefix, `/ratings` is a **suffix** rule (`relative.endsWith('/ratings')`) — tokenizes the nested rating POST while the anonymous details GET (no `/ratings` suffix) stays token-free. Updated bearer.interceptor.spec accordingly (+ explicit anonymous-details-read assertion).
- FRONTEND component `web/projects/app/src/app/consumer/rating/rating-submit.component.ts` (smart, standalone/OnPush/signals): authed-only; unauthenticated → discreet sign-in prompt wired to `AuthService.login()` (no star input). Authed → `role=radiogroup` of 5 `role=radio` real `<button>` stars (keyboard + aria-checked + accessible name "N з 5"). Submit drives shared `RequestState` (`state.run(op.execute(...).pipe(tap(success-side-effect)))`): on the success notification reflects `succeeded` + emits ONE `rating_given` via `AnalyticsEmitterService` (Step 8 `events.ratingGiven`, restaurant id + session id only, no geo/PII); failure surfaces typed `ApiError.messageKey` localized via `translateText`, emits nothing. Honest smoothed-note (`consumer.rating.smoothedNote`, invariant #6). Wired into `restaurant-details.component.ts` after the menu list.
- i18n: added `consumer.rating.*` block to `uk.catalog.ts` (title/region/star/submit/submitting/success/smoothedNote/signInPrompt/signIn/chooseScore). en stays empty placeholder.
- TESTS: new `rating-submit.component.spec.ts` (sign-in gate; login wired; accessible radiogroup; submit posts `{score}` to right id + emits rating_given on success only; no-score guarded; 400 → localized typed error + no rating_given; smoothed-note honesty). Updated `data-access.spec.ts` (+2: submitRating method/URL/body/no-self-bearer + 400 typed failure), `bearer.interceptor.spec.ts` (rating suffix match + anonymous details read), `restaurant-details.component.spec.ts` (added AuthService stub + SUBMIT_RATING provider for the new child).
- OUTCOMES: backend `dotnet build src/WhereToEat.sln` 0/0; Ratings.Application unit 6/6. Frontend `npm run build` clean (pre-existing soft budget warnings only), core 327/327 (35 files), app 209/209 (33 files), lint clean, prettier clean. Step left [~] in-progress for qa.

## Step 22 reviewer fixes (round 2)
- `rating-submit.component.ts` `select()`: now calls `this.state.reset()` (RequestState.reset clears data+error) so a stale 400 alert is cleared when the user picks a different star. Spec: error gone after re-select.
- `chooseScore` i18n key wired as visible hint `[data-testid="rating-choose-hint"]`, shown `@if (selected() === null)` inside the signedIn branch (explains the disabled submit). Style `.dp-rating__hint` (caption, on-surface-variant). Spec: hint visible+submit disabled when no star; hint gone+submit enabled after select.
- PublicApi integration `RatingEndpointTests.Submit_PublishesRatingGiven_OnSuccess`: builds a DEDICATED derived factory `_fixture.Factory.WithWebHostBuilder(ConfigureTestServices(WithRatingPublishHarness))` — does NOT touch the shared `PublicApiFixture`, so other PublicApi tests are unaffected. `WithRatingPublishHarness` removes every MassTransit-assembly descriptor (host's AddMessaging) then `AddMassTransitTestHarness` re-registering the two cross-cutting consumers; asserts `harness.Published.Any<RatingGiven>(...)`. Compile-only here (Docker unavailable). Added `MassTransit` + `MassTransit.TestFramework` pkg refs and Messaging + Contracts project refs to the PublicApi integration csproj.
- Outcomes: backend build 0 warn/0 err; Ratings.Application.UnitTests 6/6. Frontend core 327/327, app 211/211, lint clean, prettier clean, build OK (pre-existing result-card + portal-shell budget warnings, unrelated).

## Step 23 (backend: analytics rollup-reader extension + admin-scoped read endpoint) — RECONCILED a partial interrupted attempt + completed
- PRE-EXISTING (from interrupted attempt, VERIFIED coherent + correct, left as-is): the Application + Infrastructure layers were already done and build-clean.
  - `IAnalyticsRollupReader.cs` (Analytics.Application): added 5 aggregate methods + DTO records — `GetRestaurantTrafficAsync`→`RestaurantTrafficSummary`, `GetDemandBreakdownAsync(top)`→`DemandBreakdown`(Categories/Dishes of `DemandCount`), `GetPricePositioningAsync`→`PricePositioning`(`DishPricePosition`), `GetRatingsDistributionAsync`→`RatingsDistribution`, `GetConversionFunnelAsync`→`ConversionFunnel`. Interface already `public` (no visibility change needed, per plan).
  - `AnalyticsRollupReader.cs`: Dapper impls; shared `QueryStageCountsAsync` (DRY: traffic+funnel are projections of the same 3 grouped counts); safe `Ratio` (0 not NaN); `top<=0` returns empty (avoids invalid `TOP (@Top)`).
  - `AnalyticsSql.cs` (stays `internal static`): grouped queries verified vs schema — `SelectRestaurantTraffic` (Kind 0/2/3 SUM-CASE filtered by `$.restaurantId`), `SelectDemandByCategory`/`SelectDemandByDish` (Kind 4 Search, `OPENJSON($.categoryIds|$.dishIds)` cross-apply, TOP(@Top)), `SelectPricePositioning` (`catalog.MenuItem` OUTER APPLY `recommendation.PriceMedian` with AreaKey '*' fallback — NO event data), `SelectRatingsDistribution` (`ratings.RatingAggregate` ScoreCount/ScoreSum). JSON keys camelCase (writer uses `JsonSerializerDefaults.Web`) — matches `JSON_VALUE`/`OPENJSON` paths. EventKind ordinals match (0=Impression,2=CardOpen,3=Action,4=Search). All aggregates-only — no actor hash / per-event id / coordinate / per-user row by query shape (invariant #11).
- THE REAL GAPS I FILLED:
  - `AddAnalyticsReadModule(connectionString)` in `AnalyticsInfrastructureServiceCollectionExtensions.cs`: slim read-only registration — `TryAddSingleton<ISqlConnectionFactory>` + scoped `IAnalyticsRollupReader→AnalyticsRollupReader`, and deliberately NONE of the ingest/anonymizer stack (writer/salt/anonymizer/handler). Mirrors AddAnalyticsPersistence vs AddAnalyticsModule.
  - `src/Hosts/WhereToEat.AdminApi/Endpoints/AnalyticsDashboardEndpoints.cs` (new, `internal static`): 5 GET endpoints under `/admin/analytics/...`, ALL `.RequireAuthorization(AdminAuthorization.PolicyName)`. `traffic`/`funnel` (restaurant + window), `demand` (window + `top`, area-wide), `price-positioning` + `ratings` (restaurant, no window). `TryValidateWindow` → descriptive 400 `{error,message}` (`Analytics.Window.Required|Inverted|TooLarge`, MaxWindow 366d) BEFORE the reader — never a 500. `ClampTop` bounds demand TOP (default 20, max 100). Returns aggregate DTOs as JSON (Results.Ok).
  - Admin host wiring: `Program.cs` adds `using ...Analytics.Infrastructure.DependencyInjection`, calls `AddAnalyticsReadModule(connectionString)` in the module block, and `app.MapAnalyticsDashboardEndpoints()`. csproj: added Analytics.Application + Analytics.Infrastructure ProjectReferences (host = the place allowed to compose module Infrastructure). The admin host registered NO analytics module before — this is the fix that makes `IAnalyticsRollupReader` resolve there.
- TESTS:
  - `tests/Unit/WhereToEat.Analytics.Application.UnitTests/AddAnalyticsReadModuleTests.cs` (NEW, no Docker): registers reader+factory; does NOT register the ingest stack (IAnalyticsEventStore/IAnonymizer/ISaltProvider/IngestEventCommandHandler); rejects blank conn string. Project total 28/28 (was 23).
  - `tests/Integration/WhereToEat.Analytics.Integration/AnalyticsDashboardRollupTests.cs` (NEW, Docker-gated): traffic counts+CTR (other venue's events don't bleed in); funnel stage counts+ratios; empty-window zeroed-not-NaN; demand grouped counts by category/dish + capped; non-positive top → empty no-query; price-positioning venue-vs-median + null-median-no-delta; ratings distribution avg + zeroed-when-unrated; a by-shape reflection test that `RestaurantTrafficSummary` has NO identifying property.
  - `tests/Integration/WhereToEat.AdminApi.Integration/AnalyticsDashboardEndpointTests.cs` (NEW, Docker-gated): anonymous→401, non-admin→403, admin→200 aggregates (+ no identifying JSON field), funnel/ratings/price-positioning/demand 200, missing/inverted/overlong window→400 with the right `error` code.
- WARNINGS-AS-ERRORS gotchas fixed: inline `new[]{...}` in repeated assertions → `static readonly string[]` fields (CA1861); pure reflection test made `void` not `async Task` (CS1998); Dapper `ExecuteAsync` seed helpers typed `Task<int>` not `Task` (CA1859).
- OUTCOMES: `dotnet build src/WhereToEat.sln` 0 warn / 0 err. Analytics.Application.UnitTests 28/28; ArchitectureTests 34/34 (new host→Analytics ref + read-DI placement honor all boundary rules). Both Docker-gated integration projects COMPILE + are wired but could NOT run (Docker unavailable in this env — `docker info` fails). Step left [~] in-progress for qa.

## Step 24 (operator analytics dashboard — wired to Step 23 admin reads)
- DATA-ACCESS (core): `data-access/admin/analytics-dashboard.operations.ts` — 5 ports + Http adapters, all via `ApiConfig.adminUrl(...)` so the Step-6 bearer interceptor attaches on the admin prefix:
  - GET_TRAFFIC `/admin/analytics/restaurants/{id}/traffic?from=&to=`; GET_FUNNEL `.../funnel?from=&to=`; GET_DEMAND `/admin/analytics/demand?from=&to=&top=` (top omitted when undefined; area-wide, NOT restaurant-scoped); GET_PRICE_POSITIONING `.../price-positioning` (no window); GET_RATINGS_DISTRIBUTION `.../ratings` (no window).
  - DTOs mirror `IAnalyticsRollupReader` records in camelCase (minimal-API default policy) in NEW `domain/analytics-dashboard.models.ts`: `RestaurantTrafficSummaryDto`/`ConversionFunnelDto`/`DemandBreakdownDto`+`DemandCountDto`/`PricePositioningDto`+`DishPricePositionDto`/`RatingsDistributionDto`. Aggregates-only by shape (invariant #11) — no actor/per-event/coords field anywhere; demand `selectionId` is a taxonomy id, not a person.
  - Tokens added to `data-access/tokens.ts`; barrels: domain/index, data-access/index.
- GOTCHA (test): Angular `HttpParams` does NOT percent-encode the ISO `:` in query VALUES, so the shared `flush(...)` helper (matches `urlWithParams`) needs the RAW unencoded `from=2026-..T..:..:..Z` URL, not `encodeURIComponent(...)`. Encoded expectation → expectOne finds none → request stays open → cascading `verify()` failures in later tests + other spec files (test-module-already-instantiated). Fixed by using raw URLs.
- FEATURE (app) `portal/analytics/`: smart `analytics-dashboard.component.ts` (restaurant-id + from/to datetime-local form seeded to last 30 days; converts to ISO via `new Date(v).toISOString()`; fans out 5 reads each on its OWN `LoadingService.create()` so one failing query doesn't blank the rest; pre-load hint until first load; aggregates-only privacy note for invariant #11). Dumb panels: `traffic-summary` (dl + CTR progressbar), `conversion-funnel` (stage bars aria-hidden + text counts/rates), `demand-breakdown` (two a11y tables w/ caption+scope), `price-positioning` (table; money via `CurrencyFormatter.format` by venue `currency`, `LocaleService.activeLocale()`; cheaper/dearer/equal/no-median state as TEXT not colour), `ratings-distribution` (avg+count, empty-state when 0).
- ROUTE: `analytics/analytics.routes.ts` lazy `loadComponent`; registered in `portal.routes.ts` as `path:'analytics'` `loadChildren` under the adminGuard parent. `PORTAL_NAV` analytics entry already existed (placeholder) — no nav edit needed. portal.routes.spec lazy-children list extended to include `analytics`.
- i18n: added `portal.analytics.*` to uk catalog (en stays empty placeholder → falls back to uk).
- TESTS: data-access.spec.ts new describe block (7 cases: 5 path/param/typed-response + 400 typed failure + bearer-not-on-op). Component: `analytics-dashboard.component.spec.ts` (pre-load, render all 5 families, ISO+id targeting, per-metric error isolation, loading, form-invalid gate, privacy note) + `metric-panels.spec.ts` (funnel rates, demand row+empty, price cheaper/no-median+currency, ratings empty).
- OUTCOMES: core 335/335, app 222/222, `npm run build` exit 0 (pre-existing budget WARNINGS only), lint clean, prettier clean. analytics-dashboard lazy chunk emitted.
- E2E: the "admin → portal → dashboard" Playwright journey is OWNED BY Step 26 (e2e dir intentionally empty until then; browsers not installed, needs Keycloak+backend). Dashboard reachability proven via route+component tests here (same approach Steps 19/20 used).

## Step 25 (a11y / performance / visual-polish hardening — RECONCILED a partial interrupted attempt + completed)
- RECONCILED partial pre-existing work:
  - `angular.json` budgets already raised (left as-is, now justified): `anyComponentStyle` warn 4kB→**6kB** (error kept at **8kB**), `initial` warn 500kB→**700kB**. JUSTIFICATION: the workspace convention is single-file standalone components (template + token-driven styles co-located), so shell/card components legitimately carry responsive-grid + multi-state styling that the original 4kB warn was too tight for (the warning trio portal-shell ~5.4kB / result-card ~4.5kB / app-shell ~4.3kB are all cohesive, not bloated). The **8kB error ceiling is unchanged** so it still catches genuine style bloat. Initial bundle ~650kB (uk i18n + Material + OIDC) is within a 700kB warn / 1MB error envelope. Build is now WARNING-FREE.
  - `loading.service.ts` `LoadingService.create()` had been changed to auto-wire `DestroyRef.onDestroy(() => state.destroy())` (the RequestState teardown fix) + a new spec test, but the change left 23 core tests RED: `inject(DestroyRef, {optional:true})` THROWS NG0203 when there is NO ambient injection context at all (the `optional` flag only suppresses not-found, not no-context), and most spec tests call `new LoadingService().create()` outside DI. FIXED with a private `resolveDestroyRef()` that try/catches the optional `inject` → returns `undefined` outside a context (auto-teardown simply doesn't apply there; caller owns `destroy()`). Updated the stale `RequestState.destroy()` docstring to state auto-teardown is wired when created in an injection context.
- RequestState teardown AUDIT (the substantive fix above resolves it system-wide): every `RequestState` in the app is created as a **component field initializer** via `this.loadingService.create<…>()` — i.e. inside an injection context — so they ALL now auto-tear-down via the ambient `DestroyRef` on component destroy. No component needs a hand-written `ngOnDestroy` for this. `selection-builder` + `protection-flags` already use `takeUntilDestroyed` for their other rxjs streams (unchanged). No leak remains.
- TOKENIZED remaining raw values (Step 10/19 low findings):
  - Added form-field flex-basis tokens to `_tokens.scss`: `--dp-field-basis-xs|sm|md|lg|xl` (6/10/12/16/20rem). Rewired the 7 raw `flex: … Nrem` / `min-width: Nrem` literals in `menu-edit-form` (amount→sm, currency→xs), `menu-management` (items flex→lg + min-width→md, editor→xl), `restaurant-picker` (field flex→lg + min-width→md).
  - Rewired the 4 `1px solid` hairline borders (Step 10 finding) in `category-browser` + `prefix-search` to the existing `--dp-border-width-hairline` token (was defined-but-unused). `border: 0` resets left as-is (not design-token-addressable).
- A11Y audit (both surfaces): the foundations from earlier steps verified present — global skip link (`.dp-skip-link`, WCAG 2.4.1), `prefers-reduced-motion: reduce` global media query (2.3.3), `.dp-sr-only` utility, semantic landmarks (`header`/`main`/`footer`), `focus-visible`, modal focus-trap (map-modal cdkTrapFocus + restore), dashboard charts already accessible (progressbar+aria-valuenow, `<table>`+caption+scope, funnel bars aria-hidden + text equivalents, sections aria-labelledby). GAP FIXED: the result-card RATING block read disjointly to a screen reader ("star 4.5 (12)") — added a consolidated `.dp-sr-only` phrase (`consumer.results.ratingAria` / `noRatingAria`, new uk keys) and marked the star icon + visual value/count fragments `aria-hidden` (WCAG 1.1.1). Star icon was already aria-hidden.
- PERFORMANCE audit (verified, no regression): all `@Component`s are `ChangeDetectionStrategy.OnPush` (scan: zero missing); all routes are lazy `loadComponent`/`loadChildren` (zero eager `component:` routes); prefix search debounced (`SEARCH_DEBOUNCE_MS=250`); taxonomy cached (catalog store, Step 9); generic-photo `loading="lazy"` + `decoding="async"` + responsive (fluid width + `aspect-ratio` reserves space, no CLS); long lists virtualize (CDK viewport, Step 12).
- AXE (scoped out, justified): axe-core is NOT a workspace dependency and the natural home for automated axe scans is the Playwright e2e suite, which is OWNED BY Step 26 (e2e dir intentionally empty until then; browsers not installed). The Step 25 a11y work here is the manual audit + component-test assertions on the new ARIA (result-card rating). Adding axe-core + e2e axe scans would pull Step 26 forward.
- TESTS: +2 app (result-card consolidated rating-aria label; no-rating sr phrase), +1 core already present (loading.service auto-teardown). result-card existing `result-rating` textContent assertions still pass (sr-only phrase + still-rendered aria-hidden visual fragments both contain the value/count).
- OUTCOMES: `npm run build` exit 0 **WARNING-FREE** (before: 3 anyComponentStyle warns + 1 initial-bundle advisory; after: none); `npm run lint` app+core clean; `npm run format` clean; tests core **336/336** (35 files) + app **224/224** (35 files) = **560** (was 557). Step left [~] in-progress for qa.

## Step 26 (testing & e2e closeout — coverage gates + key journeys + axe a11y)
- COVERAGE GATES via the native `@angular/build:unit-test` builder options (clean, CI-gateable, no custom vitest config): added a `coverage` test **configuration** per project in `angular.json` (`coverage:true`, `coverageReporters:[text-summary,lcovonly]`, `coverageExclude`, `coverageThresholds`). Installed `@vitest/coverage-v8@4.1.9` (matches installed vitest 4.1.9) — coverage errored "missing @vitest/coverage-v8" without it.
  - **core** (RN-blueprint business logic = higher bar): 90 stmts / 85 branch / 95 func / 90 lines. Excludes `*.spec.ts`, `public-api.ts`, `index.ts` barrels. ACTUAL: **97.1 / 93.82 / 99.63 / 97.21**.
  - **app**: 80 / 75 / 70 / 80. Excludes specs/test-providers/environments/main.ts. ACTUAL: **87.37 / 82.6 / 76.13 / 86.14**.
  - New `npm run test:coverage` script (`ng test core --configuration coverage && ng test app --configuration coverage`). VERIFIED the gate FAILS non-zero on breach (temporarily set core stmts→99.9 → builder exited 1 + `ERROR: Coverage for statements (97.1%) does not meet global threshold`; reverted).
- PLAYWRIGHT E2E (`web/e2e/`): browsers installed via `npx playwright install chromium` (env had none). 4 spec files + support:
  - `support/api-mocks.ts`: deterministic `page.route` gateway mocks for the consumer journey (categories/dishes/recommend/map/details/analytics-ingest). GOTCHA: Playwright matches routes in REVERSE registration order — broad `**/api/public/**` catch-all MUST be registered FIRST so specific endpoint mocks (registered after) win. Recommend fixture is anti-price/anti-alpha to prove backend-order preservation (#5).
  - `support/live-stack.ts`: `E2E_LIVE_STACK=1` + Keycloak/user/admin/restaurant env gating for the authed journeys.
  - `consumer-journey.spec.ts` (RUNNABLE, anonymous/mocked): browse taxonomy → add dish → match/sort/filters → recommend → results-in-backend-order → view-on-map inline (no nav away) → details w/ always-free contact links. Asserts #1/#2/#5/#6/#12/§5.8.
  - `a11y.spec.ts` (RUNNABLE): `@axe-core/playwright` WCAG2a/2aa scans on selection-builder, results-list, OPEN map-modal, details — asserts ZERO critical/serious violations. This is the home for the Step-25-deferred axe scans.
  - `rating-journey.spec.ts` + `portal-journey.spec.ts` (LIVE-STACK-GATED, `test.skip` w/ reason when unset): real OIDC Keycloak login + bearer-authed POST rating (+`rating_given` ingest assertion) / admin portal protection-flag write + analytics dashboard. Structured against relative `/api` paths so they run against `deploy/` docker-compose.
  - `e2e/tsconfig.json` + `e2e/env.d.ts` (minimal ambient `process.env` to avoid adding `@types/node`): `tsc -p e2e/tsconfig.json --noEmit` passes strict. e2e is OUTSIDE the unit-test tsconfig globs (`projects/**`) so it's not pulled into vitest, and outside `ng lint` patterns.
  - Extended `format`/`format:write` globs to `{projects,e2e}/**`. Removed now-redundant `e2e/.gitkeep`. Updated README + playwright.config comment.
- OUTCOMES: unit **561 green** (337 core + 224 app, unchanged); `test:coverage` exit 0 both gates; e2e **4 passed / 2 skipped** (skips = live-stack journeys); `npm run lint` clean; `npm run format` clean (incl. e2e); `npm run build` WARNING-FREE; backend `dotnet build` 0/0 + Ratings.Application 6/6, Analytics.Application 28/28, Architecture 34/34.
- INFRA-BLOCKED (could NOT run here, same constraint as Steps 21-23 QA): the live-stack e2e journeys (no Keycloak/backend/Docker) and the Docker-gated backend integration suites (Ratings/PublicApi/Analytics/AdminApi). All COMPILE + are wired; e2e authed specs skip cleanly.
- CI (reviewer MEDIUM fix): added `.github/workflows/ci.yml` (GitHub Actions, no prior workflows in repo). Single `frontend` job, triggers push + pull_request, `working-directory: web`, Node 22 + `npm ci`, then `test:coverage` (both gates fail job on breach → enforces core gate in CI), `lint`, `build`, `npx playwright install --with-deps chromium`, `e2e`. `CI: 'true'` env so playwright honors `forbidOnly`/retries. Header comment notes a future live-stack/Docker job for the gated e2e + backend Testcontainers suites. YAML validated (python yaml.safe_load OK). Re-ran all CI-invoked commands locally: 561 unit green, core 97.1/93.82, app 87.37/82.6, lint/build green, e2e 4 passed / 2 skipped.
- Step left [~] in-progress for qa.
