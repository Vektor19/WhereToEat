# QA Notes — frontend-app

## Step 26 (done — 2026-06-26)
- **Coverage gate — core** (thresholds 90/85/95/90): Statements 97.2%, Branches 94.14%, Functions 99.63%, Lines 97.21% — all thresholds met with large margin. Gate is real and enforcing (non-zero exit on breach), configured in `angular.json` `core:test:coverage`. 35 suites / 337 tests green.
- **Coverage gate — app** (thresholds 80/75/70/80): Statements 87.37%, Branches 82.6%, Functions 76.13%, Lines 86.14% — all thresholds met. 35 suites / 224 tests green. Total 561 unit tests.
- **`npm run test:coverage`**: exited 0. Both projects' coverage summaries printed by v8. Core held to the higher bar (90/85/95/90) vs app (80/75/70/80) — configured thresholds different per project as required.
- **e2e**: 4 passed / 2 skipped (total 6 tests). Passed: consumer happy-path (build selection → recommend → results → view on map → details), axe a11y selection builder, axe a11y results+map modal, axe a11y restaurant details. Skipped: `rating-journey.spec.ts` and `portal-journey.spec.ts` — both skip via `test.skip(!liveStackEnabled, LIVE_STACK_SKIP_REASON)` with a human-readable reason; neither fails nor fakes a pass. Live-stack env var `E2E_LIVE_STACK=1` not set (correct for this environment).
- **Consumer journey assertions verified end-to-end**: backend order preserved (#5), no `≈`/`оновлено` on cards (#12), smoothed rating visible (#6), inline map without nav (§5.7), always-free contact links (#10), two-level taxonomy browse (#1/#2).
- **Axe a11y**: zero critical/serious WCAG2a/2aa violations on selection builder, results+map modal, restaurant details screens. `@axe-core/playwright` 4.12.1 installed.
- **CI workflow** (`.github/workflows/ci.yml`): on push+PR, working-directory `web/`, Node 22, `npm ci → test:coverage → lint → build → playwright install → e2e`. All four gates wired. Live-stack journeys correctly skip in CI (no `E2E_LIVE_STACK=1` in CI env).
- **Lint**: `ng lint` — "All files pass linting" for both `app` and `core`.
- **Build**: clean, 0 errors, 0 advisory warnings. Initial bundle well under budget. All feature routes are lazy chunks (confirmed in build output).
- **Backend unit+arch tests**: 319 tests across 18 suites (SharedKernel, Ratings.Domain, Analytics.Domain, Recommendation.Domain, Catalog.Domain, Contracts, Monetization.Domain, Admin.Domain, Analytics.Application, Admin.Application, Users.Domain, Catalog.Application, Parsing, Geo, Parsing.E2E, Ratings.Application, Monetization.Application, Worker, ArchitectureTests) — all 0 failures.
- **Infra-blocked (Docker/Testcontainers)**: `WhereToEat.PublicApi.Integration`, `WhereToEat.Ratings.Integration`, `WhereToEat.Analytics.Integration`, `WhereToEat.AdminApi.Integration` all fail with "Docker is either not running or misconfigured" — consistent with every prior QA pass. The Testcontainers suites compile, are wired correctly, and run when Docker is available. Step 26 is NOT failed on this: the acceptance criteria explicitly calls out that these suites are "Docker-gated backend Testcontainers suites (Steps 21–23) can't run without Docker/Keycloak here."
- **No `any`**: confirmed by developer; scope unchanged from Steps 21–25.
- **No regressions**: all invariants unchanged. No product behaviour change in this step.

## Step 25 (done — 2026-06-26)
- **Build**: `ng build` warning-free. Initial bundle 650.23 kB raw (< 700kB warn ceiling); anyComponentStyle budgets 6kB warn / 8kB error. 0 advisory warnings.
- **Tests**: `ng test core` 35 suites / 337 tests green; `ng test app` 35 suites / 224 tests green. 561 total, all passing.
- **Lint**: `ng lint` — "All files pass linting" on both `app` and `core`.
- **LoadingService.create() teardown**: `resolveDestroyRef()` wraps `inject(DestroyRef, { optional: true })` in try/catch to handle no-injection-context safely. `onDestroy` wired per `create()` call so each owner's teardown is independent. Multi-instance isolation test (`keeps two concurrent owners isolated`) present and green.
- **Tokenization**: `--dp-field-basis-xs/sm/md/lg/xl` and `--dp-border-width-hairline` defined in `_tokens.scss`. No raw `rem` flex-basis or `border: 1px` literals found in portal/consumer component styles.
- **A11y — result-card rating**: `.dp-sr-only` consolidated phrase, visual star icon + value + count all `aria-hidden="true"`. Test confirms phrase carries value+count; unrated branch has distinct "no rating" SR phrase.
- **A11y — shell/landmarks**: skip link (`<a class="dp-skip-link">`) present on consumer shell (`href="#dp-main-content"`) and portal shell (`href="#dp-portal-content"`); `<main>` with `tabindex="-1"`, `<header>`/`<footer>` semantic landmarks.
- **A11y — reduced-motion**: `@media (prefers-reduced-motion: reduce)` global rule kills transitions/animations.
- **A11y — map modal focus-trap**: `cdkTrapFocus`, ESC closes via host listener, dialog title focused `afterNextRender`, focus returned to invoking element on close. `role="dialog" aria-modal="true"` with `aria-label`.
- **A11y — dashboard charts**: `<dl>` for scalar stats; `role="progressbar"` with `aria-valuenow/min/max` on CTR bar; `<table>` with `<caption>` and `scope="col"` headers for demand breakdown; price-positioning uses text labels not colour-only. Confirmed present in Step 24 notes.
- **Perf**: all routes lazy (`loadComponent`/`loadChildren` throughout). OnPush on every component confirmed (build output shows lazy chunks per feature). CDK virtual scroll above `VIRTUALIZE_THRESHOLD=20`. `debounceTime(250)` on prefix-search input. Catalog store caches categories/dishes (no-op on repeated load). `loading="lazy" decoding="async"` on all generic photo `<img>`.
- **Scope**: axe-core not added (correctly deferred to Step 26). No backend changes. No new features.
- **Contrast**: `tokens.contrast.spec.ts` asserts all body-text pairs ≥ 4.5:1 and focus-indicator pairs ≥ 3:1 — green in this run.

## Step 24 (done — 2026-06-25)
- Build: `ng build app` succeeds; same 4 pre-existing budget warnings (initial-bundle, portal-shell, result-card, app-shell) — all Step 25 concerns. New lazy chunk `analytics-dashboard-component` (514 bytes — the shell route; actual component is inline) confirmed. Lint (`ng lint`) and Prettier both pass cleanly.
- Tests: `ng test core` 35 suites / 335 tests + `ng test app` 35 suites / 222 tests = 557 total — all green. 7 new Step 24 tests in `analytics-dashboard.component.spec.ts`; 4 new panel tests in `metric-panels.spec.ts`; 7 new data-access unit tests in `data-access.spec.ts` (getTraffic, getFunnel, getDemand×2, getPricePositioning, getRatingsDistribution, 400-typed-failure, bearer note). All new suites green.
- Data-access paths verified (HttpTestingController): `GET /api/admin/admin/analytics/restaurants/{id}/traffic?from=&to=` (window), `GET /api/admin/admin/analytics/restaurants/{id}/funnel?from=&to=` (window), `GET /api/admin/admin/analytics/demand?from=&to=[&top=]` (optional cap), `GET /api/admin/admin/analytics/restaurants/{id}/price-positioning` (no window), `GET /api/admin/admin/analytics/restaurants/{id}/ratings` (no window). Double `/admin/admin/` is correct: gateway prefix `/api/admin` + endpoint path `/admin/analytics/...`.
- DTOs match Step 23 backend records field-for-field (camelCase). All 5 response types have zero per-user/per-event/coordinate fields by shape: `RestaurantTrafficSummaryDto`, `ConversionFunnelDto`, `DemandBreakdownDto`/`DemandCountDto`, `PricePositioningDto`/`DishPricePositionDto`, `RatingsDistributionDto` — invariant #11 enforced by the type shape itself.
- No `any` anywhere in Step 24 production files (grep confirmed).
- 5 InjectionTokens (GET_TRAFFIC, GET_FUNNEL, GET_DEMAND, GET_PRICE_POSITIONING, GET_RATINGS_DISTRIBUTION) registered in `tokens.ts`; all exported through `data-access/index.ts` → `public-api.ts`; components import via `from 'core'` only.
- Dashboard container: smart component, OnPush, independent `RequestState` per metric family (one failure doesn't blank others — test confirmed). Pre-load hint → form → grid lifecycle. `restaurantId` required + from/to window form; `datetime-local` values converted to ISO instants before API call. `loaded` signal guards pre-load hint.
- Privacy note (`data-testid="analytics-privacy"`) present and asserted by test (invariant #11).
- Metric panels verified: `TrafficSummaryComponent` (`<dl>` + CTR progress bar with `role=progressbar`, `aria-label`, `aria-valuenow/min/max`); `ConversionFunnelComponent` (funnel bars `aria-hidden="true"` + text equivalents in label+count spans); `DemandBreakdownComponent` (accessible `<table>` with `<caption>` + `scope="col"` headers for each dimension); `PricePositioningComponent` (table with cheaper/dearer text labels — not colour alone); `RatingsDistributionComponent` (`<dl>` with average+count, empty state when ratingCount=0).
- Design tokens: `--dp-column-min-width-sm` (8rem), `--dp-column-min-width-md` (14rem), `--dp-column-min-width-lg` (18rem) all defined in `_tokens.scss`. All component styles use only `var(--dp-*)` CSS custom properties — no raw hex/px/rem literals.
- uk i18n: all `portal.analytics.*` keys present and verified in `uk.catalog.ts` (`portal.analytics.title`, `.intro`, `.privacyNote`, `.restaurantLabel`, `.restaurantHint`, `.from`, `.to`, `.load`, `.preLoad`, `traffic.*`, `funnel.*`, `demand.*`, `price.*`, `ratings.*`). No hardcoded strings anywhere.
- Route gating: `path: 'analytics'` registered as `loadChildren` in `portalRoutes` (inherits `canActivate: [adminGuard]` + `canActivateChild: [adminGuard]` from parent — no re-declaration needed). Nav item at `PORTAL_NAV[7]` links to `'analytics'` with label key `'portal.nav.analytics'`.
- Consumer↔portal boundary: no consumer file imports from portal; no portal file imports from consumer. ESLint boundaries rules enforced; `ng lint` passes.
- Scope: no Step 25/26 features pulled forward. No backend changes (Step 23 already done).

## Step 23 (done — 2026-06-25)
- `dotnet build src/WhereToEat.sln --configuration Release -p:TreatWarningsAsErrors=true`: clean, 0 warnings, 0 errors.
- Unit tests (28/28 green): `AddAnalyticsReadModuleTests` (3 — registers reader+factory, omits ingest stack, rejects blank conn string); `AddAnalyticsModuleGuardTests` (6); `AnonymizerTests` (9); `IngestEventCommandHandlerTests` (4); `RotatingSaltProviderTests` (4); others (2).
- Architecture tests (34/34 green): all module-boundary, layer-dependency, EF-containment, and composition tests pass. `Application_DependsOnNoInfrastructureAndNoHost` passes — `IAnalyticsRollupReader` stays in Application and `AnalyticsRollupReader` in Infrastructure. `AddAnalyticsReadModule` lives in Infrastructure DI extensions, not Application.
- Docker integration tests (env-limited): Docker Desktop is installed and reported by `docker info`, but the Linux-engine named pipe (`dockerDesktopLinuxEngine` / `docker_engine`) is not accessible from the Bash shell context. Testcontainers-based suites (`WhereToEat.Analytics.Integration` and `WhereToEat.AdminApi.Integration`) cannot be run here — same environment constraint as Step 21. This is consistent with prior QA passes.
- Invariant #11 shape (no-leak) verified by inspection for all 8 aggregate record types: `RestaurantHourlyRollup`, `RestaurantTrafficSummary`, `DemandBreakdown`, `DemandCount`, `PricePositioning`, `DishPricePosition`, `RatingsDistribution`, `ConversionFunnel`. None carries `ActorHash`, `UserId`, `SessionId`, `Latitude`, `Longitude`, `Geohash`, `CoarseGeohash`, or `EventId` — invariant #11 cannot be breached by shape.
- SQL correctness verified by inspection: `COALESCE(SUM(...),0)` on all stage counts in `SelectRestaurantTraffic` (guarantees non-null longs when set is empty); `QuerySingleAsync` (not `QuerySingleOrDefaultAsync`) used for this scalar-aggregate query — consistent with the COALESCE guarantee. `AnalyticsSql` stays `internal static`. EventKind ordinals: 0=Impression, 2=CardOpen, 3=Action, 4=Search confirmed matching the domain enum.
- Slim read DI confirmed: `AddAnalyticsReadModule` registers only `ISqlConnectionFactory` (TryAdd) + `IAnalyticsRollupReader → AnalyticsRollupReader` — NO `IAnalyticsEventStore`, `IAnonymizer`, `ISaltProvider`, `IngestEventCommandHandler`. Admin host calls `AddAnalyticsReadModule` in `Program.cs` (line 47). `AnalyticsDashboardEndpoints` wired at line 99.
- Endpoint policy: all 5 endpoints call `.RequireAuthorization(AdminAuthorization.PolicyName)`. Window validation returns `{error, message}` 400 (not 500) for missing/inverted/overlength windows. `top` parameter is clamped (1-100, default 20). `top<=0` guard in reader short-circuits before SQL.
- Scope: no schema migration, no EF change, no ingest stack in Admin host, no frontend (Step 24) pulled forward.

## Step 21 (done — 2026-06-25)
- `dotnet build src/WhereToEat.sln`: clean, 0 warnings, 0 errors.
- Unit tests (`WhereToEat.Ratings.Application.UnitTests`): 6/6 pass — create path (AddAsync called, UpdateAsync not, RatingGiven published once), revise path (same object updated in place, AddAsync not called, RatingGiven published), score out-of-range returns `Validation/Rating.ScoreOutOfRange` and neither persists nor publishes, revise-to-out-of-range also fails without publishing.
- Architecture tests (34/34): `Application_DependsOnNoInfrastructureAndNoHost` passes — `WhereToEat.Ratings.Application.csproj` references only Domain/SharedKernel/Contracts; `AddRatingsApplication` DI extension lives in `Ratings.Infrastructure`, not in `Ratings.Application`. `Domain_DependsOnNothingInSolutionExcept_SharedKernel` passes.
- Integration tests (`RatingPersistenceTests`): Docker daemon is not accessible in this process context (named pipes `docker_engine` / `dockerDesktopLinuxEngine` both unavailable via Testcontainers). The 3 RatingPersistenceTests tests compile and are correctly wired (sharing `SqlServerFixture` with the existing aggregate suite). Logic verified by inspection: `SelectRatingByRestaurantAndUser` + `InsertRating` + `UpdateRating` SQL is parameterized, `UQ_Rating_Restaurant_User` constraint backed by Update (keyed by Id) not Insert on revise path — no duplicate possible.
- Scope invariants confirmed: Worker `AddJobDependencies` calls `AddRatingsPersistence` only (unchanged, line 93 of `WorkerCompositionRoot.cs`); no HTTP endpoint added (no `*Rating*.cs` under `PublicApi/Endpoints`); no EF migration or schema change (SQL constants only in `RatingsSql.cs`); no frontend wiring.
- SLN: `WhereToEat.Ratings.Application` (project `{F4C773C6}`) and `WhereToEat.Ratings.Application.UnitTests` (`{9DFEC056}`) and `WhereToEat.Ratings.Integration` (`{06769444}`) all present in NestedProjects. Reviewer's low-severity sln finding confirmed fixed.

## Step 20 (done — 2026-06-25)
- Build: `ng build app` succeeds with the same 4 pre-existing budget warnings (initial-bundle 639 kB > 500 kB, portal-shell.component SCSS, result-card.component SCSS, app-shell.component SCSS) — all Step 25 concerns, no new warnings, no errors.
- Tests: 32 app suites / 202 tests + 35 core suites / 324 tests = 526 total, all green. Lint (`ng lint` both projects) and Prettier (all `.ts` files) all pass cleanly.
- Step 20 suite coverage confirmed: `spec-app-portal-verified-verified.component` (7 tests) + `spec-app-portal-ads-ad-placement.component` (7 tests) — both visible in vitest output and passing. `data-access.spec.ts` covers `grantVerified`, `revokeVerified`, `createAdPlacement` endpoints with `HttpTestingController` assertions.
- Endpoint wiring (source-inspected and test-confirmed):
  - Grant: `GRANT_VERIFIED.execute(venueId, tier)` → `PUT /admin/venues/{id}/verified` with body `{tier: VERIFIED_TIER_ORDINALS[tier]}` (`Basic→1`, `Pro→2`); 204 → success. Tier ordinals mapped via `VERIFIED_TIER_ORDINALS` const in `admin-bodies.ts`; no `JsonStringEnumConverter` on Admin host requires integer binding.
  - Revoke: `REVOKE_VERIFIED.execute(venueId)` → `DELETE /admin/venues/{id}/verified` with null body; 204 → success.
  - Ad create: `CREATE_AD_PLACEMENT.execute(venueId, body)` → `POST /admin/venues/{id}/ad-placements` with `{targetingKey, startsAt, endsAt}` (ISO-8601 strings); 201 `{id}` → confirmation strip shows placement id.
- Invariant #10 compliance (critical — verified against uk.catalog.ts source):
  - Verified: `portal.verified.partnerNote` = `'Verified — це позначка партнерського / підтвердженого статусу (заклад керує своєю карткою, може додати реальні фото та меню «від власника»). Це не «знак якості від нас» — органічний рейтинг від статусу не залежить.'`. Test assertions `toContain('партнерського')` + `toContain('не «знак якості від нас»')` pass against the live catalog text (not a mock).
  - Ads: `portal.ads.labeledNote` = `'Це платний рекламний слот — він завжди позначається «Реклама» та візуально відокремлений від органічної видачі. Органічний рейтинг не продається; розміщення не впливає на органічне сортування.'`. Test assertions `toContain('платний рекламний слот')` + `toContain('«Реклама»')` + `toContain('Органічний рейтинг не продається')` pass.
- Date-range validation: `dateRangeValidator` group-level validator rejects `startsAt >= endsAt` (`{ dateRange: true }` on the group). Tests: reversed window → `form.invalid = true`, equal window → `form.invalid = true`, both produce zero HTTP calls. Valid window (`startsAt < endsAt`) → ISO conversion: `new Date(raw.startsAt).toISOString()` confirmed via test asserting `body.startsAt = new Date('2026-07-01T10:00').toISOString()`.
- No billing UI: only the management write actions are present (grant/revoke/create). No payment-processor code, no billing form, no stripe/payment imports anywhere in portal tree.
- Modular/portal structure: both features are `loadChildren` lazy routes (`verified-routes`, `ads-routes` chunks visible in build output) under the `adminGuard`-gated parent. Both routes are registered under `portalRoutes` at paths `'verified'` and `'ads'`. Both nav items added to `PORTAL_NAV`.
- Typed forms + validation: `ReactiveFormsModule` + `Validators.required` on all fields; `form.invalid` guard before HTTP calls; `[disabled]` binding on buttons. `SaveFeedbackComponent` reused for in-progress/success/error states.
- No `any` in any Step 20 production file (verified by grep).
- All styles via `var(--dp-*)` CSS custom properties only — no raw hex/spacing/color literals.
- All copy via `| translate` pipe with uk catalog keys — no hardcoded strings.
- Scope: no consumer imports from portal tree; no analytics dashboard (Step 24) or backend (Steps 21–23) code pulled forward.

## Step 19 (done — 2026-06-25)
- Build: `ng build app` succeeds; same 4 pre-existing warnings (initial-bundle, portal-shell.component, app-shell.component, result-card.component) — all Step 25 concerns, no errors.
- Tests: 30 app suites / 188 tests + 35 core suites / 324 tests = 512 total, all green. 30 new Step 19 tests across 6 spec files (menu-management×5, protection-flags×5, address-management×6, photo-permission×4, save-feedback×4, restaurant-picker×3).
- Lint: both `app` and `core` pass `ng lint` cleanly. Prettier: all portal `.ts` files pass.
- Endpoint wiring confirmed by source inspection:
  - `MenuManagementComponent` injects `EDIT_MENU_ITEM` → `PUT /admin/menu-items/{id}` with `{dishId, priceAmount, priceCurrency, weight?}`.
  - `ProtectionFlagsComponent` injects `SET_MENU_ITEM_DO_NOT_PARSE` → `PUT /admin/menu-items/{id}/do-not-parse` `{value}`, `SET_RESTAURANT_DO_NOT_UPDATE` → `PUT /admin/restaurants/{id}/do-not-update` `{value}`.
  - `AddressManagementComponent` injects `EDIT_ADDRESS` → `PUT /admin/restaurants/{id}/address` `{addressLine, city?}`.
  - `PhotoPermissionComponent` injects `SET_PHOTO_PERMISSION` → `PUT /admin/photos/{id}/permission` `{value}`.
  - All 5 endpoint paths are composed via `ApiConfig.adminUrl()` (no hardcoded host/prefix). 204 is success; typed `ApiError` surfaces on failure.
- Invariants honored: #3 (protection copy explicitly names parser-protection role; menu edit as source-of-truth); #7 (address edit shows re-geocode note in template, asserted by `shows the re-geocode note (invariant #7)` test); #8 (photo permission default off, `form.value: { value: false }` asserted by `defaults the permission off` test); #2 (re-categorize via Category→Dish taxonomy picker in `MenuEditFormComponent` — `categoryChange` output drives dish loads; test asserts recategorize sends new dishId).
- Modular/portal integration: each feature is an independent `loadChildren` lazy child route under the `adminGuard`-gated parent (`portalRoutes`). `PORTAL_NAV` has entries for menu/protection/address/photos. Shared `SaveFeedbackComponent` + `RestaurantPickerComponent` (DRY). Typed reactive forms with `Validators`. Loading/disabled/success/error states all present. Material 3 with `var(--dp-*)` tokens throughout; no raw hex/color literals.
- Optimistic toggle (protection flags): success sets `{value, saving:false, error:undefined}`; failure reverts to `{value:!value, saving:false, error:err}`. Tests confirm revert and that the opposite item is unaffected. `takeUntilDestroyed(this.destroyRef)` prevents stuck in-progress on destroy.
- Save-feedback priority: error branch is checked before success branch (template: `@else if (error())` before `@else if (saved())`). `prefers the error over the saved state` test asserts no false success when both set.
- Scope/boundaries: no consumer→portal or portal→consumer import in production source (grep confirmed). No Step 20/24 features pulled forward (no `verified/` or `ads/` directory). No `any` in portal files.
- uk i18n: all portal.menu.*, portal.protection.*, portal.address.*, portal.photos.*, portal.save.*, portal.picker.*, portal.nav.menu/protection/address/photos keys present in `UK_CATALOG` (source-inspected).
- Build chunks: `menu-management-component` (46.56 kB), `protection-flags-component` (7.08 kB), `address-management-component` (5.44 kB), `photo-permission-component` (3.62 kB) — all separate lazy chunks, not in initial bundle.
- Minor low findings: `flex: 1 1 16rem` / `min-width: 14rem` in inline component styles are raw rem values (layout flex-basis, not design-token addressable). Same pattern as Step 10 low finding — below blocking threshold; Step 25 concern.

## Step 18 (done — 2026-06-24)
- Build: `ng build app` succeeds; 4 style-budget warnings — `portal-shell.component.ts` (new, Step 25), `app-shell.component.ts`, `result-card.component.ts` (pre-existing Step 25), and initial-bundle-size (non-blocking). No errors.
- Tests: 24 app suites / 158 tests + 35 core suites / 324 tests = 482 total, all green. No regressions vs Step 17.
- Portal suite counts: `portal.routes.spec.ts` 8 tests (lazy-chunk check, guard identity on canActivate+canActivateChild, admin-admitted, non-admin-denied, anon-denied, plus 3 child deep-link inheritance tests via `routesWithTestChild`); `portal-shell.component.spec.ts` 7 tests (nav skeleton, signed-in identity, logout wired, login wired, non-admin denied state with role=alert + no nav + identity still shown, skip link, no "updated" copy).
- Lazy separation: build output shows `portal-shell-component` (37.84 kB) and `portal-home-component` (1.11 kB) as distinct lazy chunks separate from `consumer-routes` chunk. Portal is NOT in the initial bundle.
- Role-gated boundary: `canActivate: [adminGuard]` + `canActivateChild: [adminGuard]` both wired on the parent route; tests verify both hooks use the exact same `adminGuard` function from `core` (not a duck-typed re-implementation).
- Consumer-portal boundary: ESLint `boundaries/element-types` rule disallows consumer→portal and portal→consumer (`consumer` only allows `['consumer','shared','core']`; `portal` only allows `['portal','shared','core']`). Confirmed no actual cross-imports in production source files. `ng lint` passes cleanly on both projects.
- No `any` in portal files (grep confirmed).
- UK catalog: all `portal.*` keys present (brand, skipToContent, navLabel, signIn, signOut, nav.*, home.*, denied.*).
- Token: `--dp-layout-portal-nav-width: 16rem` added to `_tokens.scss`; shell styles use only `var(--dp-*)` tokens (no raw hex/spacing).
- Shell: `PortalShellComponent` — top bar with brand + identity (`sub` + roles) + login/logout wired to `AuthService`; Material side-nav with data-driven `PORTAL_NAV`; content `<router-outlet>`; accessible (skip link `a.dp-skip-link → #dp-portal-content`, `role=alert` denied state, `[attr.aria-label]` on sidenav, `tabindex="-1"` on content main for skip-link focus target); OnPush; all copy via `| translate` pipe.
- Scope: no Step 19/20/24 management features pulled forward. No `any`. Clean from all prior invariants.

## Step 17 (done — 2026-06-24)
- Build: `ng build app` succeeds; 3 warnings — 2 pre-existing `anyComponentStyle` budget on `app-shell.component.ts` + `result-card.component.ts` (Step 25), and the initial-bundle-size warning (non-blocking). No errors.
- Tests: 35 core files / 324 tests + 22 app files / 143 tests = **467 total, all green**. Three i18n-specific core suites confirmed in run: `spec-i18n-locale.config`, `spec-i18n-locale.service`, `spec-i18n-in-memory-translate.loader`. Language-switch app suite `spec-app-shell-language-switch.component` confirmed green.
- Lint + Prettier: both `app` and `core` projects pass `ng lint` cleanly; Prettier reports no violations on all `.ts/.html/.scss`.
- Translate mechanism: `InMemoryTranslateLoader.getTranslation('uk')` resolves known keys to uk strings; unknown locale `'zz'` yields `{}` so ngx-translate falls back to the `uk` fallback (not a crash, not a raw-key leak).
- Deferred keys: test `'the uk catalog carries every deferred error / rating / geo key'` asserts 13 specific leaf strings (all `errors.*`, `rating.lowReviewNote`, `rating.noReviews`, all `consumer.geo.error.*`) are string-typed in the uk catalog. Verified against catalog source directly.
- Language switch: `LanguageSwitchComponent` drives `LocaleService.use()` → `translate.use()` + `document.documentElement.lang` + `active` signal + `localStorage` persistence. `use('fr')` (unsupported) is silently ignored — active stays unchanged. Tested via `LocaleService` unit suite + `LanguageSwitchComponent` component spec.
- Locale-aware formatting: `DistanceFormatter.formatKm(1.2, 'uk')` vs `formatKm(1.2, 'en-US')` produce different output (comma vs dot decimal) — test `'is locale-aware (decimal separator differs by locale)'` confirms. `CurrencyFormatter.format()` uses `Intl.NumberFormat` driven by `PriceCurrency` — no hardcoded `₴`. `ResultsListComponent` and `RestaurantDetailsComponent` both inject `LocaleService` and pass `activeLocale()` to formatters.
- No hardcoded user-facing strings: scanned all consumer/shell `.ts` files for literal Ukrainian/English strings outside catalogs — none found. All copy goes through `| translate` pipe or `translateText()`/`translate.instant()` with a catalog key.
- `translateText` helper: centralizes the `Signal<string>` cast with documented justification; used in `ErrorStateComponent`, `RestaurantDetailsComponent`, `GeoOptInComponent`, `MapModalComponent`, `ResultCardComponent`.
- `LOCALE_ID` pinned to `uk` + documented: static provider, Angular built-in pipes use `uk` formatting always; Intl-based `core` formatters follow `LocaleService.activeLocale()`. `registerLocaleData(localeUk)` called in `app.config.ts`.
- `@angular/compiler` JIT polyfill: `angular.json` `app:build:test` config adds `polyfills: ["@angular/compiler"]`; `providersFile: "projects/app/src/test-providers.ts"` injects the real uk catalog globally. Component tests assert translated text, not raw keys — confirmed real catalog is in use (not a mock).
- Additive-locale proof: `en` catalog is `{}` (empty scaffold); `SUPPORTED_LOCALES` has both `uk` + `en` entries; adding a real locale requires only a catalog file + a `SUPPORTED_LOCALES` entry — no code change.
- Scope/boundaries: i18n in `core/src/i18n/` (barrel-exported via `public-api.ts`); `LocaleService` is Angular but the catalog/config is framework-light. No `any` in any i18n file. No portal (Steps 18+) or backend (Steps 21+) code pulled forward.

## Step 16 (done — 2026-06-21)
- Build: green; 2 pre-existing `anyComponentStyle` budget warnings on `app-shell.component.ts` and `result-card.component.ts` only (Step 25 concern). Lint+Prettier clean. 308 core tests (32 suites) + 139 app tests (21 suites) = 447 total — all green.
- Single path: all consumer production source files (`map-modal`, `restaurant-details`, `selection-builder`, `query-controls`, `results-list`) inject `AnalyticsEmitterService` only. `INGEST_ANALYTICS` appears in consumer spec files only as test setup for the emitter mock — confirmed no direct ingest call in production consumer code.
- Best-effort/non-blocking: `flush()` clears buffer (`this.buffer = []`) before calling ingest, so a slow/failed flush cannot block or retry-storm. `catchError(() => EMPTY)` swallows failures silently; dropped not retried. `.subscribe()` fire-and-forget — no `await` or throw back to caller.
- Flush triggers: 5 triggers all tested — size threshold (`FLUSH_BATCH_SIZE=20`), debounce timer (`FLUSH_DEBOUNCE_MS=5000` via `vi.useFakeTimers`), `visibilitychange→hidden` (test: `document.visibilityState='hidden'` → dispatch event), `pagehide` (test: `window.dispatchEvent(new Event('pagehide'))`), and injector destroy (flush called in `destroyRef.onDestroy`).
- `KNOWN_KINDS` guard: unknown kind (e.g. `'geo'`) dropped before buffer push — test asserts batch total=1 with only `'view'` after emitting `'geo'` + `'view'`.
- Session event: `emitSessionStart()` exported from `app.config.ts`, wired via `provideAppInitializer(emitSessionStart)`. Reads `GeoOptInStore.optedIn()` + `SelectionStore.userGeo()` and calls `events.session({geo})` when opted in, `events.session({})` otherwise. No `geo` kind anywhere.
- Geo fields on search/session events (not a kind): `selection-builder.emitSearch()` spreads `{geo}` only when `selection.userGeo() !== undefined`. `event-builders.geoFields()` returns `{latitude, longitude}` or `{}`. All 11 event builder tests pass; `'never produces a "geo" kind'` test and `'attaches geo as latitude/longitude FIELDS'` test both green.
- `resultSetToken` omits `userGeo`: `{userGeo: _omitted, ...identity} = request`; `JSON.stringify(identity)` used as dedupe key. Toggling geo opt-in does not change the token → same scope → no re-impression.
- Impression dedupe: `emitImpressionsOnce('set-1', set1)` called twice → 2 impressions total (not 4); new token `'set-2'` resets scope → 3rd impression emitted for `r-1`. Test green.
- Privacy: `base()` in event builders stamps `kind`, `occurredAtUtc`, `sessionId` (opaque UUID from `SessionIdProvider`). `userId ?? null` asserter returns `null` on all events. No PII.
- No `any` in analytics or consumer source files (only in comment prose).
- Scope: emitter in `core/src/analytics/` (RN-portable). Consumer features in `consumer/` tree. No backend steps (21+) pulled forward.

## Step 15 (done — 2026-06-21)
- Build: green; 2 pre-existing `anyComponentStyle` budget warnings (Step 25 concern, non-blocking). Lint+Prettier clean on both app and core. 299/299 tests across 31 suites — all green.
- Default OFF / no auto-acquire (invariant #11): `GeoOptInStore` seeds from `localStorage` defaulting `false`; `GeoOptInComponent` constructor calls `acquireAndApply` only when `optInStore.optedIn() && selection.userGeo() === undefined` — zero calls on fresh session. Test `'is off by default and does NOT call geolocation until opt-in'` asserts `geo.calls === 0` + `userGeo === undefined`.
- clearGeo omits userGeo (invariant #11): `SelectionStore.clearGeo()` destructures `{ userGeo: _omitted, ...rest }` and calls `setState(rest)` — the key is truly absent. `buildRequest()` spreads `...(state.userGeo === undefined ? {} : { userGeo })`. Test `'toggling off clears userGeo so the built request OMITS it'` asserts `'userGeo' in build.request === false`. Toggle-on test asserts `build.request.userGeo === { latitude:50.45, longitude:30.52 }` with range-valid coords.
- No `geo` EventKind: `EventKind` union has exactly 8 members (impression/view/card_open/action/search/filter/rating_given/session). `analytics.models.spec.ts` line 31 asserts `EVENT_KINDS.not.toContain('geo')` and `Object.keys(EVENT_KIND_WIRE_NAME).not.toContain('geo')`. No `geo` kind emitted anywhere.
- Coords not persisted / opt-out removes key: `GeoOptInStore.writeStored(false)` calls `store.removeItem(GEO_OPTIN_STORAGE_KEY)` — no literal `'false'` stored. Test `'opts out by removing the storage key (privacy-clean)'` asserts `localStorage.getItem(key) === null`. Coords live only in `SelectionStore` (in-memory signal, re-acquired on re-init).
- Graceful failure: all 4 error codes (1=PERMISSION_DENIED, 2=POSITION_UNAVAILABLE, 3=TIMEOUT, unsupported API) return typed `GeolocationFailure` with i18n message key — service never rejects or throws. Component reverts toggle to off on failure, shows error toast from key. Silent-mode init re-acquire (persisted opt-in, permission revoked) reverts to off with no error toast shown.
- Seam/a11y/tokens/scope: `GeolocationService` and `GeoOptInStore` in `core/src/geo/` (barrel-exported). Consumer toggle in `consumer/geo/` importing via `from 'core'` (ESLint boundary clean). Material `mat-slide-toggle` provides label+ARIA+keyboard natively. All styles via `var(--dp-*)` tokens (no raw values). `enableHighAccuracy: false` (low-accuracy request). No `any`. No Step 16 batching emitter or backend work pulled forward.

## Step 14 (done — 2026-06-21)
- Build: green; 2 pre-existing `anyComponentStyle` budget warnings on `app-shell.component.ts` and `result-card.component.ts` only (Step 25 concern). New lazy chunk `restaurant-details-component` (11.56 kB) confirmed. Lint: both `app` and `core` clean. Prettier: all detail files clean.
- Tests: 3 new spec files, 22 new tests (restaurant-details×9, contact-links×7, menu-list×6), all green. Full suite: app 19 suites/125 tests + core 29 suites/291 tests — all green, no regressions.
- Always-free contact links (§5.8/#10): `ContactLinksComponent` is dumb (no Verified gate, no store). Smart container maps every `ContactLinkDto` unconditionally. Empty/whitespace label fallback uses `link.label?.trim().length ? link.label : fallbackLinkText()` — kind-derived fallback avoids empty aria-label (WCAG 4.1.2). Web links get `target="_blank" rel="noopener noreferrer"`; phone links (`tel:` or kind=`phone`) get no `target` attribute. Empty list renders discreet `[data-testid="contact-links-empty"]` note, never a paywall.
- Menu (invariants #8/#12): `MenuListComponent` dumb — renders pre-formatted strings only. Prices formatted in container via `CurrencyFormatter.format(amount, priceCurrency)` — no hardcoded `₴`; EUR test asserts price not containing `₴`. Weight rendered only when not null. Generic photo default: `photo: { isReal: false, src: 'generic-dish.svg' }` hardcoded in `toMenuItem()` — no real-photo signal in read model. No `≈`, no "орієнтов", no "оновлено" in any template (asserted in tests).
- Route/states: `restaurant/:id` registered in `consumerRoutes` via lazy `loadComponent`. `withComponentInputBinding()` confirmed in `app.config.ts`; only one `input()` on container (`id`) — no clobbering risk. States: loading (shared `LoadingComponent`), error (shared `ErrorStateComponent` with retry), 404 (typed not-found error → error state), loaded (details article). `results-list.component.ts` `onOpen()` calls `router.navigate(['restaurant', vm.restaurantId])` — test asserts navigation.
- Dish-name resolution: plain non-computed scan of `catalog.state().dishesByCategory.values()` in `resolveDishName()` — no computed-in-computed (NG0600-safe). Neutral fallback `'Страва'` for unknown dish ids. Both paths tested.
- Analytics: `view` + `card_open` pair emitted once per load via `effect()` guarded by `viewEmitted` signal. `action` emitted on `(linkClick)` output from `ContactLinksComponent`. All best-effort (`catchError → EMPTY`).
- a11y/tokens/scope: `<nav aria-label>` + semantic `<h2>` headings. All styles via CSS custom properties (no raw values). Smart/dumb separation clean. No `any`, no portal imports, no `core` deep-path imports, no Step 15+ geo or Step 21+ rating code pulled forward.

## Step 13 (done — 2026-06-21)
- Build: green; 2 pre-existing `anyComponentStyle` budget warnings on `app-shell.component.ts` and `result-card.component.ts` only (Step 25 concern, not Step 13). Lint clean (both core + app projects). Prettier clean on all map files.
- Tests: 29 core suites (288 tests) + 16 app suites (103 tests) — 391 total, all green. New suites from Step 13: `deep-link.provider.spec.ts` (5 tests), `google-embed.provider.spec.ts` (5 tests), `map-provider.selection.spec.ts` (5 tests), `map-modal.component.spec.ts` (9 tests). Pre-existing suites unchanged.
- Swappable abstraction: `MapProvider` is a pure port interface (just `resolve(payload): MapView`), no Angular/Google specifics. `MAP_PROVIDER` InjectionToken in core. `GoogleEmbedMapProvider` reads key from `inject(APP_CONFIG).map.googleMapsEmbedKey` — never hardcoded. `selectMapProvider()` is a pure function (no DI) choosing Embed when `provider === 'google-embed' && key.length > 0`, deep-link otherwise. Consumer modal only injects `MAP_PROVIDER` — no Google class name in modal.
- Live, nothing cached (§5.7/#6/#7): `MapModalComponent.fetch()` calls `this.getMap.execute(id)` on every open (no store, no cache). Providers are pure shaping functions — no I/O, no state. No Google rating field touched anywhere in Step 13 code.
- Degradation/no-map: empty key → `selectMapProvider` returns DeepLink. `GoogleEmbedMapProvider.resolve()` degrades to deepLink.resolve() when no key or no embeddable target. `DeepLinkMapProvider.resolve()` returns `{kind:'none'}` when `hasMapData === false`. Modal renders `<app-error-state emptyMessage>` for `none` state. Test `'renders the no-map state for a hasMapData=false payload'` confirms.
- iframe security: sandbox `allow-scripts allow-same-origin` (required by Maps Embed; documented anti-pattern justified in source-level JSDoc with explicit warning against copying). `referrerpolicy="no-referrer"`. `embedSrc` uses `DomSanitizer.bypassSecurityTrustResourceUrl` with app-constructed URL (not user input).
- Inline open from card (§5.7): `result-card.component.ts` has separate `viewMap` output (not `open`). `results-list.component.ts` handles `(viewMap)="onViewMap($event)"` and renders `<app-map-modal>` inline — no router.navigate() call. `onOpen()` (for details) is distinct from `onViewMap()` (for map).
- Modal a11y: `role="dialog"`, `aria-modal="true"`, `[attr.aria-label]="title()"` on dialog element. `cdkTrapFocus` from `A11yModule`. `(keydown.escape)` host listener calls `requestClose()`. Backdrop click only closes when `event.target === event.currentTarget`. `onMapClose()` calls `this.mapInvoker?.focus()` — focus returns to invoker. `afterNextRender` focuses `#dialogTitle` (has `tabindex="-1"`). All styles via CSS custom properties (no raw values). `--dp-color-scrim` token added to `_tokens.scss`.
- Analytics: `actionEmitted` signal guards: set `true` on first open inside `effect()`; reload path checks `!this.actionEmitted()` → no re-emit. Test `'does not re-emit the action event when reload re-fetches the map'` explicitly asserts count stays at 1 after `reload()`. One `action` event with `restaurantId` on initial open.
- Scope/boundaries: map provider in `core/src/map/` (own barrel + `public-api.ts` export). Modal in `consumer/map/`. Modal imports `from 'core'` (barrel only) — no portal imports, no `any`, no Step 14+ (details) or Step 15+ (geo) code.

## Step 12 (done — 2026-06-21)
- Build: green with 2 `anyComponentStyle` budget warnings only — `result-card.component.ts` (Step 12, expected) and `app-shell.component.ts` (pre-existing Step 2). Both warnings, not errors; flagged for Step 25 hardening pass.
- Tests: `ng test core` 273/273 (unchanged) + `ng test app` 90/90 (25 new: results-list×14, result-card×9, ad-slot×3 — minus previously-counted). Lint + Prettier all clean.
- Invariant #5 (backend order): `ResultsListComponent.cards` maps `results.restaurants()` 1:1 via `.map()` in index order — no sort call anywhere in the results tree. Test `'renders cards in the exact backend order'` submits `[Alpha@300, Bravo@100, Charlie@200]` (anti-alphabetic, anti-price) and asserts names `['Alpha','Bravo','Charlie']` unchanged. Coverage rendered as muted `.dp-card__coverage` caption with CSS `color: var(--dp-color-on-surface-variant)`.
- Invariant #12/§10: No `≈`, no "орієнтов", no "неточ" in result-card template. Price renders as `basketPrice` string (pre-formatted by `CurrencyFormatter.formatOptional`). Test asserts `cardText.not.toContain('≈')` passes. `basketPriceCurrency='EUR'` test confirms no hardcoded `₴`.
- Invariant #6: `RatingPresenter.present()` drives `RatingPresentation`; `LOW_REVIEW_NOTE_KEY` and `NO_REVIEWS_KEY` from `core` barrel. No-reviews branch (`hasRating:false`) renders em-dash `—` + "Поки немає відгуків." note (not "(0)"). Low-confidence branch renders count in note. Test `'shows the low-review note below threshold and not above'` confirms exactly 1 note for 2-review/80-review pair.
- Invariant #11 (distance): `DistanceFormatter.formatOptionalKm(null)` returns `null`; card conditionally renders `@if (vm().distance !== null)`. Test with mixed `distanceKm: 1.2` / `null` pair confirms exactly 1 distance element.
- Invariant #10 (ad-slot): `isAd` defaults `false` in `toViewModel()`. No ad-slot rendered in organic flow (test `'does not render an ad slot for the organic backend flow'`). `AdSlotComponent` renders `[data-testid="ad-slot"]` with class `dp-ad` (bordered/tinted), `[data-testid="ad-label"]` containing "Реклама", and `aria-label` containing "Реклама". Re-emits open from inner card.
- Invariant #8 (generic photo): `photo: { isReal: false, src: GENERIC_PHOTO_SRC }` hardcoded in `toViewModel()` — no backend `realPhotoUrl` field consumed yet. `GenericPhotoComponent` uses `data-testid="generic-photo"` when `!isReal`. `loading="lazy"` and `decoding="async"` on `<img>`. Test confirms `[data-testid="generic-photo"]` present by default, `[data-testid="real-photo"]` absent.
- States: Loading → `<app-loading>`, error → `<app-error-state>`, empty → `<app-error-state emptyMessage>`, results → `<section data-testid="results-list">`. All via shared Step 7 primitives.
- Virtual scroll: threshold 20; `> VIRTUALIZE_THRESHOLD` → CDK `<cdk-virtual-scroll-viewport data-testid="results-virtual">`; `≤ VIRTUALIZE_THRESHOLD` → `@for` inline. Two boundary tests: 21 → virtual present, 20 → virtual absent + 20 cards in DOM.
- Analytics: `effect()` fires `impression` batch on `loaded()`. `onOpen()` fires `card_open`. Both use `INGEST_ANALYTICS` injection token (best-effort `catchError → EMPTY`). Test captures `ingestBatches` and asserts impression batch has 2 events with correct `restaurantId` order + one `card_open` on click.
- Design tokens: `@use 'tokens'` in result-card styles; breakpoint via `tokens.$dp-breakpoint-sm`; all spacing/color via CSS custom props. No raw px in SCSS (px in JS comment only for CDK `itemSize` hint — acceptable).
- No `any`, no portal imports, no Step 13+ work pulled forward.

## Step 11 (done — 2026-06-20)
- Build: green (pre-existing Step 2 SCSS budget warning only). Lazy chunk `selection-builder-component` carries the new query-controls inline.
- Tests: `ng test core` 273/273 (unchanged) + `ng test app` 65/65 (18 new: match-control×3, sort-control×4, filters-control×5, query-controls×6). Lint + Prettier clean.
- Match: `MatchControlComponent` is dumb (input `value`, output `modeChange`). `andLabel = 'Комбо (І)'` and hint driven by computed: "and" → "всі обрані позиції одночасно", "or" → "хоча б одну". Test asserts both copy strings.
- Sort (invariant #5): 5-option data-driven `SortOption[]` with `explicit` flag. Explicit sorts (price/distance/rating) produce hint "Покриття … додатковий критерій" — not primary. Composite modes produce "Складений режим". `data-explicit` attribute asserted in tests. Exactly 5 `mat-button-toggle` elements verified.
- Filters (invariant #4): `FilterDescriptor[]` input-driven; `FilterKind='numeric'` union + `@if` dispatch in template for additive future kinds. Default descriptors: price (max) + rating (min). Test proves a 3-descriptor list renders 3 inputs with zero code change. Clear button appears only when filter applied; empty input emits `clear`, non-empty emits `apply`.
- Submit (invariant #1): `QueryControlsComponent` calls `selection.buildRequest()` → `tryBuild()`; invalid build surfaces typed `ValidationError[]` (role=alert, testid `query-error-<code>`); valid build goes to `results.submit()`. Submit button `[disabled]="!selection.hasSelection() || loading()"`. Wire body test asserts exact `{items, match, sort, filters}` shape. Loading state test verified.
- Analytics seam: `INGEST_ANALYTICS` injection token used directly for `filter` event (best-effort `catchError → EMPTY`). Step 16 replaces this seam. Test counts `ingestCalls > 0` on filter apply.
- Smart/dumb: `QueryControlsComponent` is smart (injects stores + INGEST_ANALYTICS); three control components are pure dumb (inputs/outputs only).
- No `any`, no portal imports, no Step 12+ UI (results-list, map, details, geo) pulled forward.

## Step 10 (done — 2026-06-20)
- Build: green, `selection-builder-component` confirmed as a lazy chunk in bundle output.
- Tests: `ng test core` 273/273 + `ng test app` 47/47 (29 new Step-10 tests across 4 spec files). Lint + Prettier clean.
- Deterministic prefix search: `SEARCH_DEBOUNCE_MS=250`, `SEARCH_LIMIT=10`, `MIN_PREFIX_LENGTH=2` — no free text ever reaches `recommend` (invariant #1). Test asserts both search ports receive limit=10 as second arg.
- Two-level taxonomy browse: CategoryBrowserComponent is pure dumb (inputs/outputs), CatalogStore feeds it via SelectionBuilderComponent (smart). addCategory adds `{categoryId}`, addDish adds `{dishId}` — exactly-one-of enforced by SelectionStore (invariant #2).
- Selection chips: SelectionSummaryComponent takes `SelectionChip[]` from container, emits remove/clear. Discriminated union `SelectionChip` makes chip-key provably correct without optional fallback.
- Signal hygiene: `CatalogStore.readDishesState` returns idle sentinel for unloaded categories — no signal writes in computeds (NG0600-safe). Test at catalog.store.spec.ts:198 asserts no entry is created and state size is unchanged.
- `dishNames` signal in SelectionBuilderComponent captures dish names at add/pick time, avoiding computed→catalog-cache read (NG0600 guard). Uses `signal<ReadonlyMap>` updated with `update()` only when value differs.
- ARIA: combobox/listbox wiring, `aria-activedescendant` resets on new result-set via `effect()`, keyboard nav (↑↓ Enter Escape), `aria-selected` on option rows, aria-labels on all interactive controls and regions.
- Shared boundaries: `eslint.boundaries.js` has `{type:'shared', pattern:'projects/app/src/app/shared/**/*'}` and rule `{from:['consumer'], allow:['consumer','shared','core']}` + `{from:['portal'], allow:['portal','shared','core']}` — consumer→shared and portal→shared both allowed; consumer↔portal cross-import still denied.
- No `any`, no raw design values beyond `1px solid var(--dp-...)` border hairlines (minor; `--dp-border-width-hairline` token exists but unused here — low finding only, below blocking threshold). All Material imports from barrel.
- Consumer route registered lazy under `''` path in `app.routes.ts`. No Step 11+ work pulled forward.

## Step 9 (done — 2026-06-20)
- All three stores extend `SignalStore<State>`: CatalogStore, SelectionStore, ResultsStore confirmed at source level.
- Encapsulation: `stateSignal` is private; `.set/.update` only called from `setState`/`patchState` base helpers — no public raw-signal mutation possible.
- CatalogStore: categories fetched once and cached (second call is no-op, test verified call count = 1); per-category dishes cached independently; failed loads (both categories and dishes) correctly not cached — retried on next call; `force` bypass verified (call count = 2).
- SelectionStore: exactly-one-of enforced (both-ids and neither-ids rejected as no-op); dedupe by id+level; match/sort/filter mutators with contract-key validation; `clearGeo` uses `setState` with destructuring to truly omit the `userGeo` key; set→clear→build asserts `'userGeo' in result.request === false` (invariant #11).
- ResultsStore: idle→loading→loaded/empty/error lifecycle verified; backend order preserved — test uses deliberately out-of-price-order `[r-3, r-1, r-2]` array and asserts exact order (invariant #5); stale-result cancellation confirmed; `request` is a readable Signal.
- Counts: 271 core tests + 18 app tests — all green; `ng lint` all pass; `ng build` success (pre-existing Step 2 SCSS budget warning only); Prettier clean.
- No `any` in state layer; all stores in `core` behind barrel; no backend `src/` changes; no feature UIs pulled forward.


## Step 7 (done — 2026-06-20)
- Error interceptor: all 7 kinds verified (400+envelope→validation, 401→unauthorized, 403→forbidden, 404→not-found, 500→server, status-0→network, 418→unknown); non-HttpErrorResponse fallback maps to network via guarded cast; i18n keys only in core (no UA/EN strings); errors re-thrown, not swallowed.
- LoadingService/RequestState: idle→loading→loaded/failed transitions confirmed; EMPTY source lands on loaded+empty (not stuck on loading — high-finding fix); destroy() cancels in-flight; stale-run cancellation confirmed.
- SessionIdProvider: stable within session, different after reset(), opaque (UUID, no PII), graceful when sessionStorage unavailable (memory fallback).
- App-layer components (ErrorStateComponent, LoadingComponent): locale fallback map lives in app layer only — core stays locale-free. Both export through shared/ui barrel.
- Interceptor order in app.config.ts: [bearerInterceptor, errorNormalizationInterceptor] — inner error normalizer sees failures first.
- No `any` in any Step 7 files; no backend src/ changes; no feature UIs or i18n catalogs pulled forward.
- Counts: 125 core tests + 18 app tests — all green; lint all pass; build success (pre-existing Step 2 SCSS budget warning only).

## Step 5 (done — 2026-06-18)
- Verified all 18 operation ports: 10 public (catalog×3, search×2, recommend, map, analytics-ingest, identity, health) + 8 admin (catalog-edit×2, protection×2, photo, monetization×3).
- URL composition confirmed: public → `/api/public/...`, admin → `/api/admin/admin/...` (proxy rewrites `/api/admin` → `""` so backend sees `/admin/...`).
- `EVENT_KIND_WIRE_NAME` maps snake_case to PascalCase C# member names; no underscores on wire — matches `Enum.TryParse` behavior.
- Verified-tier fix: `VERIFIED_TIER_ORDINALS` sends `Basic→1`, `Pro→2` matching `SubscriptionTier` enum; test covers both tiers.
- Rating-submit and analytics-dashboard read intentionally absent (Steps 22/24).
- No `any` in data-access. No backend `src/` changes.
- All checks: `ng test` 59 core + 14 app = 73 green; `ng lint` all pass; `ng build` success (pre-existing CSS budget warning unrelated).
